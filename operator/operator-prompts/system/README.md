# Operator system prompts

Versioned personality/system prompts live here, one file per version:

    operator-system-v1.txt
    operator-system-v2.txt

They are loaded at runtime by the backend (Milestone 6+), never embedded in Kotlin source.
The prompt version in use is recorded per session for debugging.

No prompt is committed yet; the first version arrives with Milestone 6.
