<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Milestone 1: microphone capture for the loopback test. Requested at runtime. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!-- Milestone 2: Bluetooth diagnostics. BLUETOOTH_CONNECT is a runtime permission on API 31+
         (bonded device names); BLUETOOTH covers API 29-30. MODIFY_AUDIO_SETTINGS for audio mode changes. -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <!-- Milestone 3: Meta Wearables toolkit registration talks to Meta AI over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />

    <application
        android:name=".OperatorApplication"
        android:label="@string/app_name"
        android:allowBackup="false"
        android:supportsRtl="true"
        android:theme="@style/Theme.Operator">

        <!-- Meta Wearables Device Access Toolkit. 0/0 = Developer Mode attestation placeholders;
             real values come from the Wearables Developer Center via local.properties. -->
        <meta-data
            android:name="com.meta.wearable.mwdat.APPLICATION_ID"
            android:value="${mwdat_application_id}" />
        <meta-data
            android:name="com.meta.wearable.mwdat.CLIENT_TOKEN"
            android:value="${mwdat_client_token}" />
        <!-- Privacy: opt out of Meta analytics and SDK crash reporting (ADR-015). -->
        <meta-data
            android:name="com.meta.wearable.mwdat.ANALYTICS_OPT_OUT"
            android:value="true" />
        <meta-data
            android:name="com.meta.wearable.mwdat.CRASH_REPORTING_OPT_OUT"
            android:value="true" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Return deep link from the Meta AI registration flow (scheme mirrors Meta's samples). -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="operator" />
            </intent-filter>
        </activity>
    </application>
</manifest>
