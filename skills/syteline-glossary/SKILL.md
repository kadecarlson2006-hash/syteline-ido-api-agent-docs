---
name: syteline-glossary
description: "Mongoose glossary of terms and definitions"
version: "1.0"
tags: [erp, syteline, infor, mongoose, ido, csi]
category: syteline
metadata:
  hermes:
    tags: [erp, syteline, infor, mongoose, ido, csi]
---

Next
End Sub
Sample global script
Sub Main()
Dim MyInt As Integer = 555
Dim MyString As String = "123"
Dim MyBoolean As Boolean = True
Dim MyDecimal As Decimal = 555
Dim MyLong As Long = 555
Dim MyDate As Date = Date.Today
Dim MyString2 As String
If Not Parameters(0).IsNull Then
MyString2 = Parameters(0).Value
End If
MyDate = Parameters(MyInt).GetValue(Of Date)()
MyDecimal = Parameters(ParameterCount).GetValueOfDecimal(55)
MyLong = Parameters(3).GetValue(Of Long)()
Dim MyNullableInt As Nullable(Of Integer) = Parameters(1).GetNullableVal
ue(Of Integer)()
For i As Integer = 0 To ParameterCount() - 1
Dim MyParm As IGlobalScriptParm = Parameters(i)
If MyParm.IsNull() Then

Infor Mongoose Application and Form Development | 1312

Misc. reference-NoTOC
MsgBox(String.Format("We know that parameter {0} is Null, so we will not
try GetValue()", i))
Dim MyNullableLong As Nullable(Of Long) = MyParm.GetNullableValue(Of
Long)()
Else
Try
MyDecimal = MyParm.GetValue(Of Decimal)()
Catch ex As System.Exception
MsgBox(String.Format("Failed GetValue for parameter {0} [{1}]", i, My
Parm.Value))
End Try
End If
Try
MyLong = MyParm.GetValueOfLong(MyLong)
Catch ex As System.Exception
If ex.Message <> "Object reference not set to an instance of an object."
Then
MsgBox(String.Format("Caught Exception during GetValueOfLong(555): {0}",
ex.Message))
End If
End Try
Try
Dim MyNullableInt2 As Nullable(Of Integer)
MyNullableInt2 = MyParm.GetNullableValue(Of Integer)()
Catch ex As System.Exception
MsgBox(String.Format("Failed GetNullableValue for parameter {0} [{1}]",
i, MyParm.Value))
End Try
Next
End Sub

Setting the Site Coordinator Configuration from a
command line
The Site Coordinator Configuration is used to coordinate servers in a server farm. You can use
ConfigManager.exe from a Command Line Interface (CLI) to specify this setting.
Note: This operation, performed from a CLI, is intended primarily for system administrators working
with customers in a cloud environment. Customers working in an on-premises installation environment
can use the Configuration Manager utility to accomplish the same ends.
Syntax
This is the command line syntax to set the Site Coordinator Configuration:
ConfigManager.exe -U:scc -mode:action -config:configurationName
where:
•

action is either add, edit, or delete.

Infor Mongoose Application and Form Development | 1313

Misc. reference-NoTOC
•

configurationName is the name of the configuration used as the Site Coordinator Configuration
by the TMS (Tenant Management Service).

Standard events generated by programmatic actions
on collections
These events are triggered during operations on IDO collections, regardless of how the operations are
triggered:
StdObjectCopy on page 394
StdObjectCopyCompleted on page 394
StdObjectDelete on page 395
StdObjectDeleteCompleted on page 395
StdObjectDeleteExisting on page 396
StdObjectDeleteExistingCompleted on page 396
StdObjectDeleteNew on page 396
StdObjectDeleteNewCompleted on page 397
StdObjectFirst on page 397
StdObjectFirstCompleted on page 397
StdObjectLast on page 398
StdObjectLastCompleted on page 398
StdObjectNew on page 398
StdObjectNewCompleted on page 399
StdObjectNext on page 399
StdObjectNextCompleted on page 400
StdObjectPrevious on page 400
StdObjectPreviousCompleted on page 400
StdObjectRefresh on page 400
StdObjectRefreshCompleted on page 401
StdObjectRefreshCurrent on page 401
StdObjectRefreshCurrentCompleted on page 401
StdObjectSave on page 402
StdObjectSaveCompleted on page 402

Infor Mongoose Application and Form Development | 1314

Misc. reference-NoTOC
StdObjectSaveCurrent on page 402
StdObjectSaveCurrentCompleted on page 403
StdObjectSelectCurrent on page 403
StdObjectSelectCurrentCompleted on page 403

Standard events generated by user actions on the
Actions menu
These events are related to a user's selection of a standard operation command on the Actions menu:
StdFormCopy on page 370
StdFormCopyCompleted on page 370
StdFormCurrentNotes on page 371
StdFormCurrentNotesCompleted on page 371
StdFormDelete on page 372
StdFormDeleteCompleted on page 373
StdFormFilter on page 374
StdFormFilterCompleted on page 375
StdFormFilterInPlaceBegin on page 375
StdFormFilterInPlaceBeginCompleted on page 375
StdFormFilterInPlaceCancel on page 375
StdFormFilterInPlaceCancelCompleted on page 376
StdFormFilterInPlaceExecute on page 376
StdFormFilterInPlaceExecuteCompleted on page 377
StdFormFirst on page 378
StdFormFirstCompleted on page 378
StdFormGraph on page 379
StdFormGraphCompleted on page 379
StdFormLast on page 381
StdFormLastCompleted on page 381
StdFormLinkUpdate on page 381
StdFormLinkUpdateCompleted on page 382

Infor Mongoose Application and Form Development | 1315

Misc. reference-NoTOC
StdFormNew on page 383
StdFormNewCompleted on page 384
StdFormNext on page 384
StdFormNextCompleted on page 385
StdFormNotes on page 385
StdFormNotesCompleted on page 385
StdFormPerformValidations on page 386
StdFormValidationsCompleted on page 393
StdFormPrevious on page 386
StdFormPreviousCompleted on page 387
StdFormRefresh on page 387
StdFormRefreshCompleted on page 388
StdFormRefreshCurrent on page 388
StdFormRefreshCurrentCompleted on page 389
StdFormSave on page 389
StdFormSaveCompleted on page 390
StdFormSaveCurrent on page 390
StdFormSaveCurrentCompleted on page 391
StdFormSpreadsheetCopy on page 392
StdFormSpreadsheetCopyCompleted on page 393

Standard events generated by various user actions
These events occur during various user actions on forms, such as clicking certain commands on the
Edit menu, closing a form, tabbing between form components, and navigating a collection:
StdAppLaunchInbox on page 359
StdAppLaunchTask on page 359
StdCurCompAdd on page 360
StdCurCompDetails on page 363
StdCurCompFind on page 365
StdCurCompGraphProperties on page 365
StdCurCompClipboardCopy on page 361

Infor Mongoose Application and Form Development | 1316

Misc. reference-NoTOC
StdCurCompClipboardCut on page 361
StdCurCompClipboardPaste on page 361
StdCurCompClipboardPasteAppend on page 362
StdCurCompClipboardPasteAppendComplete on page 362
StdCurCompClipboardPasteOverwrite on page 362
StdCurCompClipboardPasteOverwriteComplete on page 363
StdCurCompEditFlexLayout on page 364
StdCurCompEditGridColumns on page 364
StdFormCalledFormReturned on page 367
StdFormClose on page 367
StdFormColFind on page 368
StdFormColFindCompleted on page 368
StdFormColReplace on page 368
StdFormColReplaceCompleted on page 369
StdFormColSort on page 369
StdFormColSortCompleted on page 369
StdFormComponentHelp on page 369
StdFormCurCacheChanged on page 371
StdFormCurCacheChanging on page 371
StdFormExitCancel on page 373
StdFormExitOk on page 373
StdFormGetBoundValues on page 378
StdFormHelp on page 379
StdFormInitCompleted on page 380
StdFormLoadBoundValues on page 382
StdFormLoadDerivedValues on page 383
StdFormPagePop on page 385
StdFormPerformValidations on page 386
StdFormValidationsCompleted on page 393
StdFormPredisplay on page 386
StdFormPutBoundValues on page 387
StdGraphElementClicked on page 393

Infor Mongoose Application and Form Development | 1317

Misc. reference-NoTOC
StdGraphElementClickedCompleted on page 394
StdLastIMEValueUpdated on page 394
StdQueryFormAddAdditional on page 403
StdQueryFormClear on page 404
StdQueryFormInitialize on page 404
StdQueryFormRemoveAdditional on page 405
StdQueryFormSelectAdditionalProperty on page 405
StdWebContainerMessageReceived on page 405

Standard events with no default implementation
These events are notifications that an event has occurred or is about to occur. They have no default
implementation, and generating events programmatically has no effect. You can create custom handlers
for them, which execute when the events fire.
StdCurCompSort on page 366
StdFormClose on page 367
StdFormColFindCompleted on page 368
StdFormColReplaceCompleted on page 369
StdFormColSortCompleted on page 369
StdFormCopyCompleted on page 370
StdFormCurCacheChanged on page 371
StdFormDeleteCompleted on page 373
StdFormFilterCompleted on page 375
StdFormFilterInPlaceBeginCompleted on page 375
StdFormFilterInPlaceCancelCompleted on page 376
StdFormFilterInPlaceExecuteCompleted on page 377
StdFormFirstCompleted on page 378
StdFormGraphCompleted on page 379
StdFormInitCompleted on page 380
StdFormLastCompleted on page 381
StdFormLinkUpdateCompleted on page 382
StdFormNewCompleted on page 384

Infor Mongoose Application and Form Development | 1318

Misc. reference-NoTOC
StdFormNextCompleted on page 385
StdFormPredisplay on page 386
StdFormPreviousCompleted on page 387
StdFormPutBoundValues on page 387
StdFormRefreshCompleted on page 388
StdFormRefreshCurrentCompleted on page 389
StdFormSaveCompleted on page 390
StdFormSaveCurrentCompleted on page 391
StdFormSpreadsheetCopyCompleted on page 393
StdFormValidationsCompleted on page 393
StdGraphElementClickedCompleted on page 394
StdLastIMEValueUpdated on page 394
StdObjectCopyCompleted on page 394
StdObjectDeleteCompleted on page 395
StdObjectDeleteExistingCompleted on page 396
StdObjectDeleteNewCompleted on page 397
StdObjectFirstCompleted on page 397
StdObjectLastCompleted on page 398
StdObjectNewCompleted on page 399
StdObjectNextCompleted on page 400
StdObjectPreviousCompleted on page 400
StdObjectRefreshCompleted on page 401
StdObjectRefreshCurrentCompleted on page 401
StdObjectSaveCompleted on page 402
StdObjectSaveCurrentCompleted on page 403
StdObjectSelectCurrentCompleted on page 403

Templates for SSRS report definition files
Infor provides these SSRS report definition templates, which help ensure uniform report layout when
converting or creating reports.
•

Template_Report_Landscape.rdl

Infor Mongoose Application and Form Development | 1319

Misc. reference-NoTOC
•
•
•
•
•

Template_Report_Landscape_IDO.rdl
Template_Report_Portrait.rdl
Template_Report_Portrait_IDO.rdl
Template_SubReport_Landscape.rdl
Template_SubReport_Portrait.rdl

Landscape report width is 10.5 inches and portrait report width is 7.77 inches. IDO report templates
are for reports that retrieve data using an IDO, and subreport templates are for reports that are part of
main reports.

URL Parameters for General Use
When accessing the Mongoose web client, URL parameters can be used to specify what happens
when the web client opens.
This table lists and describes the URL parameters that are available for general use by anyone who
wants or needs to configure such URLs:
URL parameter (syntax)

Description

config=configName

Login parameter.
The value for configName is the name of the configuration the
user wants to log in to. When specified, this configuration is automatically selected and displays in the Configuration field on the
Sign In dialog box.

configgroup=configGroupName

Login parameter.
The value for configGroupName is the name of the configuration
group to which the named configuration belongs.
Use this option when you want to display only members of the
specified configuration group in the Configuration drop-down list
on the Sign In dialog box.

deviceid=integer

This parameter is used primarily when testing form displays. The
integer indicates which device type definition, as defined on the
Edit Device Types form, is to be used to display the form.

Infor Mongoose Application and Form Development | 1320

Misc. reference-NoTOC

URL parameter (syntax)

Description

form=formName

Session startup parameter.

form=formName(launchParams)

The value for formName is the name of the form to be opened.
This parameter can be used with or without form launch parameters
(launchParams).
For example, form=Events as a URL parameter opens the
Events form with its default settings. form=Events(FILTER(EventName='IdoOnInvoke')SETVARVALUES(InitialCommand=Refresh)) as a URL parameter with additional form
launch parameters filters the Events collection for the IdoOnInvoke
framework event and then refreshes the form (overriding the form's
default Initial Command parameter) when it opens to display just
that event.
Note: It is possible to do value filtering when launching a form
with these launch parameters.

menu=hidden

Session startup parameter.
This parameter is used to hide the main menu.

notitle=1

This parameter is used in conjunction with the page=formonly
parameter. When specified, it tells the browser not to display the
title bar for the form.

notTouch

When used, this parameter forces the web client to treat the
browser as a non-touch device. This does not necessarily imply
that touch actions will not work, only that the browser cannot assume touch-action capability.

Infor Mongoose Application and Form Development | 1321

Misc. reference-NoTOC

URL parameter (syntax)

Description

page=formonly | light |
portal

Session startup parameter.
• When the formonly option is used with this parameter, the
form opens without any of the usual WinStudio menus or
toolbars, and without access to the Explorer. This option is
intended primarily for use with forms designed for use on
mobile devices, where space is at a premium.
This option typically redirects the user to a page named
"PlainForm.aspx".
For more information, see About the Toolbar in Form-only
Mode on page 98.
•

When the light option is used with this parameter, the form
opens in a special lightweight web browser client. This, again,
is designed as a feature primarily for mobile forms and apps.
This option typically redirects the user to a page named "MobileForm.aspx" and is best used for mobile device apps.

•

When the portal option is used with this parameter, the expectation is that the app will be opened and hosted within an
iFrame.
This option typically redirects the user to a page named "PortalPage.aspx".

passcode=userPassword

Login parameter.
This parameter is used to populate the Password field on the Sign
In dialog box with the user's password.
Note: Because this value is not encrypted but is passed along as
plain text, we strongly discourage its use.

theme=themeName

Session startup parameter.
This parameter is used to override the default theme setting for
the session. The value for themeName is the name of the theme
to use.

user=userName

Login parameter.
The value for userName is the login user ID. When specified, the
user ID field on the Sign In dialog box is automatically populated
with this user ID.

useworkstation=0 or 1

Login parameter.
When this parameter is set to 1, the login process bypasses the
Sign In dialog box, and instead uses the user's workstation login
credentials to log the use in.

Infor Mongoose Application and Form Development | 1322

Misc. reference-NoTOC

URL Parameters for Specialized Use
When accessing the Mongoose web client, URL parameters can be used to specify what happens
when the web client opens.
The URL parameters addressed in this topic are intended for use only by developers and administrators
who need to configure the web client for specialized access. This access might include integrations
with other Infor web applications such as ION and Infor Ming.le; or it might include configuring access
to other external applications.
This table lists and describes these specialized URL parameters:
URL parameter (syntax)

Description

forceSSO=0 or 1

This parameter is used only in conjunction with Infor Ming.le.
When this parameter is set to 1, it implies the use of a workstation
type login for authentication. When this option is used, the user name
and password fields in the Sign In dialog box are hidden. If the configuration has also been set using a parameter, then the entire Sign
In dialog box can be bypassed.

InforCurrentLocale=locale

Data formatting locale.
This parameter is supplied by Infor Ming.le as a URL parameter, and
is used to override Mongoose locale setting.

InforCurrentLanguage=language

The language for display strings.

mgDevMode=devMode

This parameter typically is used only for testing purposes and is usually set in the web.config file. This parameter is used to modify the
source of the JavaScript.

This parameter is supplied by Infor Ming.le as a URL parameter, and
is used to override Mongoose language setting.

These are the valid values for devMode:
•
•
•
•

•

default (or blank) - This option is used for packaged builds.
This is the typical use.
nomin - This option is used for non-minified packaged builds.
dev - This option is used for standard development purposes.
This option does not work unless it is built with Sencha Cmd.
testing - This option is used to point to the build/testing directories for development. This option is empty unless it has been
built with Sencha Cmd.
production - This option is used to point to the build/production
directories for development. This option is empty unless it has
been built with Sencha Cmd.

Infor Mongoose Application and Form Development | 1323

Misc. reference-NoTOC

URL parameter (syntax)

Description

mgShowName=showWhat

This parameter typically is used only for testing purposes and is usually set in the web.config file. This parameter is used to include original
component names as HTML attributes, to assist with automated
testing.
These are the valid values for showWhat:
•
•
•
•

preventNav=yes | no |
dirty

default (or blank) - Do not include any values. This is the typical
use.
justOuter - Include only the outer, or top level, elements.
andValue - Include the outer elements and data value elements.
andGridCell - Include the outer, data value, and grid cell elements.

Parameter used primarily by Infor Ming.le interactions.
This parameter is used by Infor Ming.le, when refreshing or doing
anything else where no data has changed within the web part, to
prevent the normal prompt about navigating away from the current
page.
•
•
•

When set to yes, the prompt displays, even when the web part
is displayed inside a widget or in Infor Ming.le.
When set to no, the prompt does display, the prompt does not
display at all.
When set to dirty, the prompt displays only if the form has the
Save button enabled.

stype=full

Use this parameter to force the use of a full user session, particularly
when opening a form in FormOnly mode. This parameter should be
used only with a /session/open/ command.

tenant=tenantID

This parameter is used only in conjunction with Infor Ming.le.
The value of tenantID is the name used to identify the application as
a tenant. This is usually replaced by "Infor".
This parameter has two fundamental uses:
•
•

executionTimeout="seconds"

It can be passed to Pendo as the "tenant" value used for analytics.
It can be used for federated authentication.

This parameter is used to set the number of seconds that the system
is to wait before declaring a web connection timeout. It is typically set
in the web.config file.
The value of seconds is the number of seconds to wait. The default
for this value is 300, or 5 minutes.

Infor Mongoose Application and Form Development | 1324

Misc. reference-NoTOC

Widget options
The Responsive Form Builder allows authorized users to create forms in Runtime Mode. Among the
form types you can create are widgets, which are designed to be smaller and to function within an Infor
Ming.le™ environment. Widgets are typically simple, designed with a single, focused purpose in mind.
All widget options have these characteristics:
•
•
•
•

All widget forms are created as "FormOnly" forms.
As a rule, only two property values can be displayed. Exceptions are noted in the table below.
All widget forms include standard Minimize, Maximize, and Close buttons.
On many of the widget forms, information is displayed using tiles (tile forms).
This means that, when the widget is created, the wizard actually creates two forms: the widget
form and an accompanying tile form. Both are launched when the widget is created.
Note: When created, the tile form typically appears blank. Unless you know what you are doing
with tile forms, we recommend that you leave the tile form alone and simply close it.

This table lists and describes the various widget form options available with the Responsive Form
Builder:
Widget template

Notes/Comments

Graph

This template creates the widget with a graph that reprsesents the data
from two properties in the IDO. The graph is generated as a line graph,
with the first property represented on the X-axis and the second property represented on the Y-axis.
Alternative graph or chart types can be implemented by modifying the
form in Web Designer or Design Mode, using the Data Source > Binding
settings.

Grid

This template creates the widget as a simple grid that can display up
to two property values. The property names display in the header row,
and the data displays on the rows of the grid.

Grid with Header

This template is similar to the "Grid" option, but adds a banner bar
("header") at the top of the form. This banner, by default, is labeled as
"Form Name", but you can use the Web Designer or Design Mode to
change it to whatever you want.

Grid with Header and Actions

This template is basically the same as the "Grid with Header" option.
The difference is that, when you open this type of form in FormOnly
mode, "Filter-In-Place", Save, Add, and Delete icons/buttons are displayed; and you can modify and save editable values.

Infor Mongoose Application and Form Development | 1325

Misc. reference-NoTOC

Widget template

Notes/Comments

Grid with Search

This template is similar to the "Grid" option, but adds a Search field at
the top. This Search field allows the user to search for specified strings
in the data.
Note: When the form is first created, the Search is not functional. To
be able to use the Search functionality, you must perform a follow-up
procedure.
See Enabling Search on a widget form on page 1230.
Be aware that, even after the Search functionality has been enabled,
the user can search on only one of the two properties represented in
the grid. The other property is ignored during Search.

List

This template creates a list in which each entry can contain up to two
associated property values. The data is displayed using tiles.
Note: The only difference between this template and the "List v2"
template is the styling used for the data display. Functionally, they are
the same.

List with Icons

This template creates a list that displays an image alongside up to two
property values for each entry in the list.
This template can take up to three property values:
•

The first property on the Field Selection page is reserved for the
image, which must be a graphics file of type GIF, JPG, PNG, SVG,
or BMP.
The image might be for a product item or a corporate logo, for example.
Images can be imported or selected from the library from the Web
Designer.

•

The other two properties are used for the property values to display
for each list entry, similar to the basic "List" template.

List v2

This template creates a list in which each entry can contain up to two
associated property values. The data is displayed using tiles.
Note: The only difference between this template and the "List" template
is the styling used for the data display. Functionally, they are the same.

ListWithIcons

This template is similar to the "List With Icons" template. Functionally,
they are the same.
The primary differences include the styling used to display the data and
the fact that the Infor corporate logo (which can be changed, in the
Web Designer or Design Mode) displays at the top of the list.

Infor Mongoose Application and Form Development | 1326

Misc. reference-NoTOC

Widget template

Notes/Comments

WidgetInput

This template is used primarily to add data records to an IDO collection.
You can use up to five input fields to collect the data to add.
When this widget opens, by default it displays the data for the first
record in the IDO collection. Whether fields are editable or required (or
whatever) depends on how the property is specified in the original IDO
collection.
To create a new record for the IDO collection, click Add, provide the
requested information, and then click Save.

Infor Mongoose Application and Form Development | 1327

WebForms-NoTOC

WebForms-NoTOC

SoHo User Components
Use this form to know how to use, configure, and implement SoHo user components on Mongoose
forms. In Design Mode, you can see the UI and code implementation.
On this form, you can choose among these categories:
•
•
•
•
•
•
•

Buiding Forms on page 1328
Creating Containers on page 1329
Creating Modal Windows on page 1329
Displaying Progress on page 1329
Entering Text on page 1330
Navigating on page 1330
Providing Feedback on page 1330

Building Forms
On this form, you can see the SoHo user components that are used to build forms.
This table shows a list of components available in this category:
Components

Description

Button

Click to perform highly-used, supporting, and destructive actions, and actions that you do not want to call out visually

Calendar

Specify a date manually or select a date from the calendar

Date Picker

Specify a date manually or select a date from a popup calendar

Radio Button

Select one from a list options

Range Slider

Select a range value from the slider control

Slider

Select a value from the slider control

Spin Box

Specify a value manually or click the plus or minus button

Switch

Select to turn optional system functionality on or off

Ticks and Color Slider

Select a color for the trigger area and the text input area

Infor Mongoose Application and Form Development | 1328

WebForms-NoTOC

Components

Description

Time Picker

Specify time manually or select a specific time

Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Creating Containers
On this form, you can see the SoHo user components that are used to create containers.
This table shows a list of components available in this category:
Components

Description

Multi-Select

Select one or more options from the dropdown
list

Swaplist

Select and move options from two side-by-side
lists

Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Creating Modal Windows
On this form, you can see information on how to create message dialogs.
Message dialogs are modal dialogs that display messages requiring user attention.
Place the cursor on the information icon to know how to use and setup this component on Mongoose
forms. You can test this component through the Preview field.

Displaying Progress
On this form, you can see the SoHo user components that are used to display the progress of an
operation or task.
This table shows a list of components available in this category:
Components

Description

Progress Indicator

Identify the progress of time-consuming system operation

Wizard Slider

Series of steps in a logical order that is used to complete a complex
task

Infor Mongoose Application and Form Development | 1329

WebForms-NoTOC
Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Entering Text
On this form, you can see the SoHo user components that are used to enter text.
This table shows a list of components available in this category:
Components

Description

Rich Text Editor

Specify formatted text with no restriction on character length or number
of characters

Autocomplete

Provide search results from user input

Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Navigating
On this form, you can see the SoHo user components that are used to navigate through the sections
of a form.
This table shows a list of components available in this category:
Components

Description

Accordion

Navigate through sections of related content

Action

Use on any header component

Expandable Area

Hide blocks of content underneath the section header

Tabs

Maintain large quantity of information in a single form

Tree

Navigate through information in a split view structure

Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Providing Feedback
On this form, you can see the SoHo user components that are used in providing feedback.
This table shows a list of components available in this category:

Infor Mongoose Application and Form Development | 1330

WebForms-NoTOC

Components

Description

Rating

Rate an item on a set scale

Toast

Provide information or notifications that do not require user interaction

Place the cursor on the information icon on each page to know how to use and setup a component on
Mongoose forms. You can test these components through the Preview and Binding Test fields.

Web Designer forms
Calendar Settings
Use this form to specify settings for a Calendar component.
Note: When you select Calendar in the Binding Type drop-down list, the Binding Type field reverts
to a setting of None, and the Binding field displays calendar.(MODE(-1)). This form allows you
to modify that default setting.
This form is used only with Calendar components.
This form has these tabs:
•
•

Attributes
Activity Data

Use the Attributes tab to specify these attributes for the calendar:
Field/Option

Description

Mode

Select the viewing mode for the calendar: Day, Week, or Month.
The default is Month.

Categories

Select the categories you want to use to distinguish activities on
the calendar, using different colors.
To define a new category and color, click New. After you specify
a name for the Category and a Color (in #RRGGBBAA hexadecimal format) for items that use it, click OK.
To make changes to an existing category, click Edit.
To remove a category, click Delete.

Infor Mongoose Application and Form Development | 1331

WebForms-NoTOC

Field/Option

Description

Selected Item Variables

Use these fields to specify form variables that alert the user of
which activities have been selected.
• ID - From the drop-down list, select the variable that is to
contain the value for the activity ID.
• Start - From the drop-down list, select the variable that is to
contain the value for the start date of the activity.
• End - From the drop-down list, select the variable that is to
contain the value for the end date of the activity.
When an activity is selected, the variables are updated. For example, a user could set the Start and End variables to send an alert
when a vacation day is selected on a calendar.

Use the Activity Data tab to specify the collection of data that is used in the calendar:
Field/Option

Description

Collection

Select the form collection to which the calendar is bound.

Properties

Use the drop-down lists to select the properties to associate with
each of these fields.

Deployment
The Deployment form is used primarily to create deployable zip packages for Mongoose-based
applications or applets and then deploy the apps or items on the target systems.
This form has two main options:
•

Export using AccessAs and Object Prefix: When you click this option, these sections are
displayed on the Detail view or at the bottom portion of the form:
• Export Applet: This section is used in the source environment to specify the settings for the
app scripts that will be created and packaged for deployment.
• Deploy Applet: This section is used in the target environment to specify the configuration
where you want to deploy the application or to import a zip package for the app.
Note: You cannot import files if you are working in a Software-as-a-Service (SaaS) environment.
• Export using forms, selected items and referenced items: When you click this option, these
sections are displayed on the Detail view or at the bottom portion of the form:
• Available Items and Selected Items: These sections are used to view and select the items
to include in the scripts that will be created and packaged for deployment.
• Output options: These fields and options are used in the target environment to specify the
location where you want to deploy or import the zip package of the items.
Note: You cannot import files if you are working in a Software-as-a-Service (SaaS) environment.
Note: This form can be used to create and deploy only apps that employ XML scripts. If you want to
create and deploy apps that incorporate SQL scripts, you must use the Mongoose Deployment
Scripting Utility.

Infor Mongoose Application and Form Development | 1332

WebForms-NoTOC

Contained Components
Use this dialog box in the Web Designer to add, configure, and delete components within a container
component. This dialog box is accessed by clicking the Edit grid column(s) button in the upper left
corner of these container components while in the Web Designer:
•
•
•

Notebooks
Grids
Vertical Grids

For FlexLayout components, this dialog box is accessed by clicking the Edit FlexLayout Attributes
button in the upper left corner of the FlexLayout component, and then clicking the Contained
Components button in the FlexLayout dialog box.
This dialog box consists of two basic sections:
•

The left side contains:
• The Items column, which displays the list of the contained components currently defined for
the container.
For example, if this dialog box is accessed from a grid, the list displays, in order, all the columns
currently defined for the grid.
•
•

•

The Sequence column, which displays the sequence number of the contained components.
These controls at the bottom:
Control

Description/Comments

New button (+ icon)

Adds a contained component to the list
Note: This button is disabled when this dialog box is accessed from
a FlexLayout component.

Delete button (trash
can icon)

Deletes the selected component from the list

Move Up button (up
arrow icon)

Moves the selected component up in the list

Move Down button
(down arrow icon)

Moves the selected component down in the list

The right side displays the component Properties sheet for a selected contained component.
(Initially, it is blank.) This sheet is identical to its counterpart in the main Web Designer display.
You can configure the component in either place and the results are the same.

Edit Action
Use this dialog box to specify the "action" part of a conditional action value expression for a component.

Infor Mongoose Application and Form Development | 1333

WebForms-NoTOC

Field/Option

Description

Action Type

Specify the type of conditional action to define:
• NONE - Nothing happens. This setting is the default when you
create a new conditional action.
• Set - Sets a value for the component if the condition evaluates
to TRUE.
• SetModified - Sets a value for the component and marks the
record as modified, if the condition evaluates to TRUE.
• Enabled - Enable the component if the condition evaluates
to TRUE, or disable the component if the condition evaluates
to FALSE.
• Required - Make the component a required field if the condition evaluates to TRUE.
• Visible - Make the component visible if the condition evaluates
to TRUE, or hide the component if the condition evaluates to
FALSE.
• Toggle - Turn the component setting on or off based on the
condition setting.
• Modified - Mark the record as modified if the condition evaluates to TRUE.
• GenEvent - Generate an event if the condition evaluates to
TRUE.
• GenEventRet - Generate an event and return a value if the
condition evaluates to TRUE.
• Focus - Sets focus on the component if the condition evaluates
to TRUE. If the condition evaluates to FALSE, focus on another
component, if specified in the expression.
• ReturnValue - Sets the return value based on the conditional
action.
• Stop - If the condition evaluates to TRUE, stop execution of
any additional actions on the overall expression.
• Validate - If the condition evaluates to FALSE, display the
message specified in the expression.
See Conditional Action Syntax on page 307.

Action Value

Enter a value or, to display the Edit Value Expression form, click
the ellipsis (…) button to the right of this field.

Edit Browser Binding Specification
Use this form to establish and maintain information used to connect to a URL or to display HTML code
in a Browser component.
This form is used only with Browser components.

Infor Mongoose Application and Form Development | 1334

WebForms-NoTOC
This form includes these fields and options:
Field/Option

Description

Type

From the drop-down list, select what type of information to display
in the browser: a URL to a web page or inline HTML code.

Filename

If you selected URL for the Type, specify the full URL to a web
page, for example:
http://www.infor.com
If you selected HTML for the Type, enter here the HTML code to
be rendered, for example:
<HTML>
<BODY>Hello workd! </BODY>
</HTML>

Edit Browser File
Use this form to create and edit various kinds of resources for use as web user control assets:
•
•

Create or edit a text-based resource, such as an HTML, CSS, JavaScript, or XML snippet.
Import or export an image resource, video clip, PDF file, or other user component.

The File field is read-only and specifies the path and filename of the resource being created or edited.
The Content Type field is read-only and specifies the type of file being created, edited, or
imported/exported. It can be any of these types:
•
•
•
•
•
•
•
•
•
•
•

text/HTML
text/css
text/javascript
text/xml
image/jpeg
image/png
image/gif
image/tiff
application/pdf
video/mp4
application/user-component

The other fields and options that are available on this form depend on what Content Type is selected.
As a general rule, you can edit only text-based resources
•
•

The default option is for an text/HTML page, with a blank edit box in which to create or paste your
HTML, CSS, JavaScript, or XML code.
Image options display a preview of the image, with options to export or import image files.

Infor Mongoose Application and Form Development | 1335

WebForms-NoTOC
•
•

Other multimedia types, such as audio files, video files, and so on, do not allow you to preview the
content.
The application/user-component option displays a page on which you can define the properties,
attributes, and required resources of a User Component you are creating.

Edit Collection ID Binding
Use this form to bind a Group Box component to an IDO collection.
This form is used only with Group Box components.
This form includes these fields and options:
Field/Option

Description

Collection

This field specifies the name of the IDO collection
to bind to the Group Box component.

Disable Raised 3D Display

When selected, this option specifies that two-dimensional outlining is to be used for the group
box.
To give the group box a raised, three-dimensional
appearance when it has focus, clear this check
box.

Edit Collection Specifications for Subcollection
Use this form to specify the properties and attributes of a subcollection.
This form is used only with Subcollection Grid components.

Edit Condition
Use this dialog to specify a condition to include in a conditional action expression for a component.
Field/Option

Description

Up

This option moves the selected condition clause up in the sequence.

New

This option creates a new row (clause) in the condition.

Delete

This option deletes the selected clause.

Down

This option moves the selected condition clause down in the sequence.

Infor Mongoose Application and Form Development | 1336

WebForms-NoTOC

Field/Option

Description

Left Value

This field displays the value set for the left side of the current
clause in the conditional expression.
To open the Edit Value Expression dialog box and specify a different Value Type or Value for the current clause, click Edit Value.

Operator

This option specifies the operator to use to compare the Left Value
with the Right Value.

Right Value

This field displays the value set for the right side of the current
clause in the conditional expression.
To open the Edit Value Expression dialog box and specify a different Value Type or Value for the current clause, click Edit Value.

And With Next Clause

When this option is selected the current clause and the next clause
are joined with a Boolean AND filter. Clauses joined with AND allow
for only those conditions that meet the criteria specified in all
clauses.
When this option is cleared, the clause is defined as an OR clause.
Clauses joined by OR allow for conditions that meet any one of
the criteria specified in the clauses.

Edit Entry List
Use this dialog box to edit a list component class parameters. The panel on the left side of this dialog
box displays a list of the parameters required by a component class assigned to a component. The
right side of the dialog box has a set of buttons with these functions:
•
•
•
•
•

Up - Moves a selected parameter up in the list.
New - Launches the Edit Entry dialog box as a modal window. Use this dialog box to define the
new parameter.
Edit - Launches the Edit Entry dialog box as a modal window. Use that dialog box to edit the
selected parameter.
Delete - Removes a selected parameter from the list.
Down - Moves a selected parameter down in the list.

FlexLayout
Use this form to set the attributes and properties for FlexLayout regions.
This form is accessed by clicking the Edit FlexLayout Attributes button in the upper left corner of any
FlexLayout component or component contained within a FlexLayout component. It is used only by
FlexLayout components.

Infor Mongoose Application and Form Development | 1337

WebForms-NoTOC

Edit Font
Use this dialog box to specify a particular font and font size for a component, a component class, or a
property class extension.

Edit Inline List Entries
Use this form to create and edit Inline list sources. Inline list sources are hard-coded sets of values
that become the list of options displayed in this component.
Web Designer interprets list values as string names. If a list value corresponds to a string name, Web
Designer uses the string value as the list value. If the list value does not correspond to a string name,
Web Designer treats the list value as a literal.
You can have up to three values for each list item. You can then specify any or all of these to be
displayed in the component.

Edit LinkBy Specification
For a Grid component bound to a subcollection, use this form to set the parent-child relationships
between the primary collection and the subcollection.
This form is accessed only from the Edit Collection Specifications for Subcollection form.

Edit List Source Specifications
Use this form to set up and maintain the specifications for a list source associated with a component.
The options that display on this form depend on what Type of list source you select.
This form is accessed by clicking the ellipsis (…) button for the List Source property. It includes these
options:
If the selected Type is:

These options are available:

None

None—All options are disabled.

Infor Mongoose Application and Form Development | 1338

WebForms-NoTOC

If the selected Type is:

These options are available:

IDO Method

•
•
•
•

•

IDO Collection

•
•

•
•

•

Inline

•

•

•

IDO: From the drop-down list, select the IDO that is the source
for the list.
Properties: Specify the properties to be included as list items.
To select properties from a list, click the ellipsis (…) button.
Method Name: From the drop-down list, select the method
that is to return values for the list.
Method Parms (Parameters): Click the ellipsis button to launch
the Edit Method Parameters form, in which you can define
the parameters required by the selected method.
Columns to Display: (Enabled only if you have specified
properties in the Properties field) Specify, as integers, the
columns to display in the list. To select the properties from a
list, click the ellipsis button.
IDO: From the drop-down list, select the IDO that is the source
for the list.
Properties: Use this field to specify the properties to be included as list items. To select properties from a list, click the ellipsis
(…) button.
Filter etc.: Use this field to specify a filter for the data to be
returned.
Rec Cap Override: Use this field to specify a record cap for
the selected collection.
Note: This setting is subject to any system-wide record cap
overrides that might be in effect.
Columns to Display: (Enabled only if you have specified
properties in the Properties field) Specify, as integers, the
columns to display in the list. To select the properties from a
list, click the ellipsis button.
Entries: Use this field to specify the list of values. Each list
item can have up to three values, each of which can be either
displayed or hidden in the component. To set up the list, you
can click the ellipsis (…) button, which launches the Edit Inline
List Entries form.
Note: List items are defined in the Edit Inline List Entries
form using table columns and rows. Each row of the table
defines a list item.
Column For Value: Use this field to specify which column’s
value is to be used in selecting and displaying the list item.
The value in this field is expressed as an integer that indicates
which column’s values are to be used.
Columns to Display: Use this field to specify which columns’
values are to be displayed in the component. The values in
this field are expressed as integers that indicate which
columns’ values are to be used.

Infor Mongoose Application and Form Development | 1339

WebForms-NoTOC

If the selected Type is:

These options are available:

Script

•

•

Name and Parms: Use this field to specify the name of the
global script (and any parameters required by that script) that
is to provide the values for the list items.
Columns to Display: Specify, as integers, the columns to
display in the list.

Edit Method Parameters
Use this form to list in order the parameters required by an IDO method call. To create parameters,
click New. To modify an existing parameter, click Edit.
When creating or editing parameters, you have these options:
Field/Option

Description

Type

From the drop-down list, select the type of parameter required by
the IDO method.

Output?

When selected, this option signifies that a method call validator
outputs values to a property, variable, or component.
If the method call validator does not output values, clear this option.

Value Type

Use this field to specify the type of value required by the parameter
of the IDO method. You can specify a literal, property, variable, or
component value as a parameter.

Value

Depending on the Value Type, use this field to enter a literal value
or to select a property, variable, or component that is to supply
the value.

Edit Target

If the Value Type is Variable or Component, click this button to
edit the selected variable or component.

Edit Parameter
Use this dialog box to specify parameters for a named method action that is part of the specification
for the UPD (Update), INS (Insert, DEL (Delete), or POSTQUERY keywords.
These keywords set up custom processing in the IDO that overrides the corresponding standard
operation on the collection.
Field/Option

Description

Type

This field specifies the type of parameter required by the IDO.
You can specify a literal value or a property value.

Infor Mongoose Application and Form Development | 1340

WebForms-NoTOC

Field/Option

Description

Output

When this option is selected, it signifies that the method outputs
values.
If the method does not output values, clear this check box.

Value

This field specifies the value to be used for the parameter. What
you do here depends on the Type that has been specified:
• If you specified a literal value for the Type, then enter that literal value in this field.
• If you specified a property value for the Type, then select from
the drop-down list the property value to be used.

Edit Parameter for Application Event
Use this dialog box to edit the value of a parameter for an event handler of the response type Generate
Application Event. You can specify a literal value or the value of a variable, component, or property.
Field/Option

Description

Value Type

Select from the drop-down list the type for the parameter or validator.
Note: The Value for Validating Component option is available
only when this dialog box is accessed from the Edit Parameters
for Generate Application Event Validator dialog box. It supplies
a parameter value of %V, which maps to the component being
validated at the time of validation.

Value

What this field specifies depends on the Value Type that is specified. If Value Type is:
• Literal, then this field must contain a literal string.
• Property, then select from the drop-down list the name of
the property to use for the value.
• Variable, then select from the drop-down list the name of
the variable to use for the value.
• Component, then select from the drop-down list the name of
the component to use for the value.

Edit Target

To edit a variable or component value, click this button.

Edit Property Binding
Use this form to set up and maintain specifications for an IDO collection property to which a component
is bound.

Infor Mongoose Application and Form Development | 1341

WebForms-NoTOC
This form is used with any component that can be bound to a property.
Note: When you first set the Binding Type to Property, the system presents a warning message,
“Invalid property name property.” You can safely ignore this warning.
This form includes these fields and options:
Field/Option

Description

Collection

This field specifies the name of the IDO collection to be bound to
the current form or current grid component.

Property

This field specifies the name of the property in the IDO collection
to which the component is to be bound.

Load Just in Time

This option typically applies only to properties that contain binary
large object (BLOB) data.
When this option is selected, BLOB data is displayed only when
the component has focus.
To have the system display the BLOB data when the form is
opened, clear this check box.

Edit Property Specifications
For IDO Collection list sources, use this form to specify and order the properties to be returned by the
query.
For IDO Method list sources, use this form to specify and order the properties that constitute the
method's returned list of values.
For tree components, use this form to specify and order the properties to include in the current level
of the tree.
The left pane lists all the properties of the IDO. The right pane lists the properties included in the list
source.
•
•
•

You can add properties to include in the list source by selecting properties in the left pane and then
clicking Add.
You can remove properties from the list source by selecting properties in the right pane and then
clicking Remove.
You can reorder the properties as included in the list source using the Move Up and Move Down
buttons.

Edit Set of Conditional Actions
Use this form to create and maintain conditional action specifications for a component.

Infor Mongoose Application and Form Development | 1342

WebForms-NoTOC
If you reference variables, other components, or properties on collections on the form, Windows client
creates dependencies so that when those values change, the condition is automatically executed. For
example, you can specify that a certain component must be required when users specify a certain
value for another component.
Any existing conditional actions for the currently selected component are displayed in the left pane.
Each conditional action is displayed on a separate line.
Field/Option

Description

Up/Down

These buttons move the selected expression up or down in the
list.

New

This button opens the Edit Condition form, which allows you to
create a new expression.

Delete

This button deletes the selected expression.

Edit Action

This button opens the Edit Action form, where you can specify a
different action or value for the selected row.

Edit Condition

This button opens the Edit Condition form, where you can
specify a different condition for the selected row.

Edit Value

This button opens the Edit Value Expression form, where you
can specify a different value type and value for the selected row.

Edit Set Values
Use this form to set values in variables, properties, and/or components.
This form provides these options:
Field/Option

Description

Variables

The button launches the Edit Set Variable Values form, from which you
can add, modify, rearrange, or delete variables and their corresponding
values.
The field displays the variables and the values they are to be set to. These
variable/value pairs must use the syntax variable=value.

Components

The button launches the Edit Set Component Values form, from which
you can add, modify, rearrange, or delete components and their corresponding values.
The field displays the components and the values they are to be set to.
These component-value pairs must use the syntax component=value.

Infor Mongoose Application and Form Development | 1343

WebForms-NoTOC

Field/Option

Description

Properties

The button launches the Edit Set Property Values form, from which you
can add, modify, rearrange, or delete properties and their corresponding
values.
The field displays the properties and the values they are to be set to. These
property-value pairs must use the syntax property=value.

Edit Set Values
This dialog box displays a list of variables, components, or properties and their designated values. It
can be used with either form event handlers or validators.
•
•

If used with event handlers, it is accessed using the Set Values response type.
If used with validators, it can be used only with the In Collection, In Inline List, In List Cache, Method
Call, and Set Froim List Only types.
Note: The actual caption that appears on this dialog box depends on what option you chose on the
Event Handler Parameters form before accessing this (Variables, Components, or Properties).

Edit Set Values
Use this dialog box to set a value for a specific variable, component, or property pair selected from the
Edit Set … Values dialog box.
Use the Target drop-down list to select the item that is to receive the value specified here.
Use the Value field to specify the actual value to associate with the target. You can enter multiple
values as a comma delimited list.
If the target type is a property, you can also select an IDO collection from the Collection drop-down
list. The properties that are then displayed in the Target drop-down list are determined by the selected
Collection.

Edit Styles dialog box
Use the Edit Styles dialog box to define the desired theme attribute.
You can use this dialog box to perform these actions:
•
•
•
•
•

Specify or add a font
Specify or delete a font size
Specify a font style
Change font and background colors
Show or hide toolbar buttons

Infor Mongoose Application and Form Development | 1344

WebForms-NoTOC
•
•

Add a form or component type theme class
Delete a theme class

Edit Tree Binding
Use this form to define and maintain the specifications for a Tree component.
This form is used only with Tree components.
This form includes these tabs, each with its own fields and options:
•

The General tab:
Field/Option

Description

IDO

Use this drop-down list to select the top-level IDO collection to
which the Tree component is to be bound.
The selected IDO populates child nodes from the root level of
the tree.
You can specify a label for the top-level node using the Caption
property in the Component property sheet.
You can also specify a root node icon using the Bitmap File
Name property. The default icon represents a folder.

Custom Load Method

Use this drop-down list to select a custom load method or stored
procedure to be used when displaying the Tree component.
With a custom load method or stored procedure specified, you
cannot alter the list of properties (Properties). The list of properties is automatically populated and the Properties field is
read-only.
You can use the TMP keyword to refer to a property value.

Method Parms (Parameters)

If a custom load method or stored procedure is specified in the
Custom Load Method field, use this field to specify the method
parameters to use.
Note: When you select a method, the system automatically
populates this field with the parameters used by that method.
If a custom load method or stored procedure is not specified in
the Custom Load Method field, this field is disabled.

Properties

Use this field to specify a comma-separated list of IDO properties to include in nodes at the root level.
Use the ellipsis (…) button to launch a dialog box from which
you can choose properties.

Infor Mongoose Application and Form Development | 1345

WebForms-NoTOC

Field/Option

Description

Props To Display

Use this field to specify what property values from the Properties list are to be displayed in each node. Properties are indicated by integers.
Use the ellipsis (…) button to launch a dialog box in which you
can select which properties (specified in the Properties field)
to be displayed in the Tree.
This feature allows you to include hidden properties in nodes.
You can use the properties in linkages to the next level of the
tree, and the properties can be accessed programmatically
when the user clicks a node.

Read Mode

Use this field to set the isolation level for transactions when
loading the collection for the current level. This setting overrides
the default value for Collection Read Mode set in the Process
Defaults form. The override applies only to the current IDO in
the tree.

Prefix/Format

The prefix displays to the left of property values in nodes. You
can specify a single prefix for all property values in a node or
separate prefixes for each property value.
To use a single prefix, type a literal value or select a translatable
string from the list.

Font

Clicking this button launches a Font dialog box, in which you
can specify the font to use for the Tree component nodes.

All Levels Use Same IDO

When this option is selected, the Tree component is defined as
a recursive tree, that is, one in which each level of nodes uses
the same IDO.
When selected:
•

•

The Link By field on the Recursive Options tab is enabled
and you can specify the recursive linkage from one level to
the next on this IDO.
All options on the Specified Levels tab are disabled.

When this option is cleared, the Specified Levels tab options
are enabled, and you can specify the IDO collection for each
sublevel of the tree independently.
•

The Load Options tab:
Field/Option

Description

Filter

Use this field to specify parameters for the FILTER keyword,
which sets query criteria.

Infor Mongoose Application and Form Development | 1346

WebForms-NoTOC

Field/Option

Description

Distinct

When selected, this option applies the DISTINCT keyword to
the collection of nodes. This prevents duplicate entries in the
tree.
When this option is cleared, the tree can have duplicate entries/nodes.

Order By

Use this field to specify what properties are used in sorting and
presenting the returned data.
If multiple properties are specified here, the system uses them
in order, only applying secondary sort properties if there are
duplicate primary properties.

Group By

Use this field to specify the properties by which nodes are to
be placed into groups.

Rec. Cap Override

Use this field to specify a record cap for the selected collection.
This setting is subject to any system-wide record cap overrides
that might be in effect.

•

The Image Filename tab:
Field/Option

Description

Image

Use this field to specify a node image to represent this level of
the tree. The graphic file should be located on the client.
The default icon represents a folder.

Selected Image

•

•

Use this field to specify a node image (such as an open folder)
to indicate that a node is selected. If Image is specified and
Selected Image is not specified, Image is used in both the selected state and the non-selected state. The graphic file should
be located on the client.

The Recursive Options tab:
Note: Unless the All Levels Use Same IDO option is selected on the General tab, all options on
this tab are disabled.
Field/Option

Description

Ignore Target Level Filter
Spec

When selected, this option instructs the system to ignore the
target node’s Filter specification while looping back.

Link By

Use this field to set the linkage between levels in a recursive
tree.

Method Parms (Parameters)

Use this field to add, edit, or reorder parameters required by a
custom load method.

The Specified Levels tab:

Infor Mongoose Application and Form Development | 1347

WebForms-NoTOC
Note: If the All Levels Use Same IDO option is selected on the General tab, all options on this
tab are disabled.
Field/Option

Description

Up

Click to move a selected level up one level in the list.

New

Click to create a new level.
When you click this button, the Tree Binding For Sublevel
form is launched. Use that form to define the properties and
attributes of the new level.

Edit

Click to edit a selected existing level.
When you click this button, the Tree Binding For Sublevel
form is launched. Use that form to edit the properties and attributes of the existing level.

Delete

Click to delete a selected level.

Down

Click to move a selected level down one level in the list.

Edit Value Expression
Use this form to specify a conditional action value expression for a component.
Field/Option

Description

Value Type

This field specifies what type of value is being assigned to the
expression.

Value

This field specifies what value is assigned to the expression. When
the Value Type is:
• Literal, you must specify the value in this field. In this case,
the value is automatically enclosed in quotation marks when
the expression is returned to the parent dialog box.
• VariableValue, you can select the variable from the drop-down
list.
• ComponentValue, you can select the component from the
drop-down list.
• PropertyValue, you can first select the Collection, and then
select the property from this drop-down list.
In this case, the drop-down list does not get populated until
after you specify the Collection.

Global

This option displays only if the Value Typeis VariableValue.
When this option is selected, the Value drop-down list displays all
the global variables currently defined. Otherwise, the Value dropdown list displays all the form variables currently defined.

Infor Mongoose Application and Form Development | 1348

WebForms-NoTOC

Field/Option

Description

Collection

This field displays only if the Value Typeis PropertyValue.
After you select an IDO collection from this drop-down list, the
Value drop-down list is populated with the properties from that
collection.

Edit Variable Binding
Use this form to set up and maintain specifications for a variable to which a component is bound.
This form is used with any component that can be bound to a variable.
This form includes these fields and options:
Field/Option

Description

Variable

This field specifies the name of the variable to be
bound to the component.

Initial Value

This field specifies the initial value of the variable.
You can use keywords and their parameters to
specify values.

Clear On Refresh

When this option is selected, the value of the
variable is cleared when the component is refreshed.
To have a value specified when the component
is refreshed, clear this check box.

Refresh Value

This field specifies what the value of the variable
is to be when the component is refreshed.
You can use keywords and their parameters to
specify values.

Event Handler Parameters
Use this form to set error messages and other specifications for a form event handler.
This form consists of two sections: The top section is common to all form event response types. The
bottom section varies according to the Response > Type that is chosen on the Event Handlers tab
of the Details panel.
The top section, common to all form event response types includes these fields and options (all of
which are optional):

Infor Mongoose Application and Form Development | 1349

WebForms-NoTOC

Field/Option

Description

Error Message

Enter the message to be displayed when the event handler returns
an error. You can include translatable string names and any of the
Windows client substitution keywords.

Success Message

Enter the message to be displayed when the event handler succeeds. You can include string names and any of the Windows
client substitution keywords.

Only When Current Collection
Is

Select the name of the collection to which this event handler applies.
Normally, event handlers execute regardless of the currently active
collection or the specific collection operation that generated the
event. Use this option when you want the event to be generated
only if the designated collection is the active collection.

Only When Platform Is

Select the name of the platform (WIN=Windows, WEB, or ALL)
to which this event handler applies.
Normally, event handlers execute regardless of the currently active
platform.

KEEP

Select the attribute that will be applied to the KEEP parameter of
the event handler:
• FIRST - The event handler appears at the beginning of the
event handler list. If there are multiple event handlers with this
attribute, they appear in the same order that they appear in
the list.
• LAST - The event handler appears at the end of the event
handler list. If there are multiple event handlers with this attribute, they appear in the same order that they appear in the
list
• AFTER - The event handler follows the next NON-KEEP event
handler that precedes it.
• BEFORE- The event handler precedes the next NON-KEEP
event handler that follows it.

Ignore Failure

Select this option to classify any result returned by this event
handler as a success. Use this setting if you want subsequent
event handlers to continue to execute even if this event handler
returned a failure.
Otherwise, clear this check box. Use this setting if you want the
failure of this event handler to stop the execution of subsequent
event handlers.

Message on Status Line?

Select this option to display the error and success messages in
the application’s Status bar.
To have these messages display in a pop-up dialog box, clear this
option.

Infor Mongoose Application and Form Development | 1350

WebForms-NoTOC

Field/Option

Description

Disabled

Select this option to temporarily disable the event handler. Windows client does not run a disabled event handler, even if its other
execution criteria are satisfied.
To re-enable the event handler, clear this option.

For the fields and options associated with the various response types, see these topics:
For the Response Type:

See this topic:

Add entry to the Audit Log

Edit Audit Message Entry on page 1352

Binary Value Action

Event Handler Parameters - Edit Binary Value Action on
page 1353

Collection Copy

Event Handler Parameters - Edit Collection Action… on
page 1353

Collection Delete
Collection Init with Auto Insert Row
Collection Current
Collection Navigate
Collection New
Collection Notes
Collection Notes for Current
Collection Refresh
Collection Refresh Current
Collection Save
Collection Validate
Conditional Action

•
•

Edit Condition on page 1336
Edit Set of Conditional Actions on page 1342

Execute EXE or other program

Edit the Command to Execute on page 1354

Form Script Method

Event Handler Parameters - Edit Script Name and Parameters on page 1354

Generate Application Event

Event Handler Parameters - Edit Generate Application Event
Parameters on page 1355

Generate Event

Event Handler Parameters - Edit Event to Generate on page
1356

Get Location From Browser

Event Handler Parameters - Get Location Optional Arguments on page 1356

Goto Explorer Folder

Event Handler Parameters - Edit Goto Explorer Folder on
page 1356

Goto Form Page

Select Form Page on page 1357

Infor Mongoose Application and Form Development | 1351

WebForms-NoTOC

For the Response Type:

See this topic:

Goto Form

Event Handler Parameters - Specify Form Run Options on
page 1357

Goto URL

Event Handler Parameters - Edit the URL on page 1358

Inline Script

Set Inline Script Language on page 1358

Invoke User Control

This response type is not supported at this time.

Load Collection

Event Handler Parameters - Edit Load Collection Parameters on page 1359

Method Call

Event Handler Parameters - Edit Method Call on page 1360

Print Preview

This response type is not supported at this time.

Prompt

•
•

Run Background Task

Edit Background Task Name and Parameters on page 1362

Run Form

Event Handler Parameters - Specify Form Run Options on
page 1357

Run Form as Linked Child

Edit Prompt on page 1360
Event Handler Parameters - Prompt Message on page
1361

Run Form as Modal Child
Run Form to Retrieve Files
Run Script

Event Handler Parameters - Edit Script Name and Parameters on page 1354

Send Web Container Message

Event Handler Parameters - Edit Message to Web Container
on page 1363

Set Values

Edit Set Values on page 1364

Timer

Event Handler Parameters - Edit Timer Action on page 1365

Edit Audit Message Entry
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Add Entry to the Audit Log.
The fields and options specific to this response type include these:
Field/Option

Description

Message Type

Use this field to indicate the type of message to be written to the
Audit Log.
By default, messages about users logging on are numbered with
single-digit integers.
Custom messages are numbered 10,000 and greater.

Infor Mongoose Application and Form Development | 1352

WebForms-NoTOC

Field/Option

Description

Log Description

Use this field to specify a description for the log entry in the Audit
Log.
This description accompanies the Message Type and displays in
the Log Description column of the Audit Log.

Event Handler Parameters - Edit Binary Value Action
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Binary Value Action.
The fields and options specific to this response type include these:
Field/Option

Description

Action

Use this drop-down list to select the kind of action that is to be
performed on the Binary Large OBject (BLOB) data when this
event is generated.
You can choose to import, export, view, or delete the data.

Silent

When this option is selected, the system checks to see whether
the file designated in the Filename field exists. If so, it performs
the designated Action without further prompting. If not, it prompts
the user for the filename and location before carrying out any action.
When cleared, this option instructs the system to prompt for a
filename and/or location whenever the event is generated.

Component Bound to Binary
Value Property

Use this drop-down list to select the component that is to be bound
to the BLOB data.

Filename

Use this field to specify the filename of the BLOB file to be either
imported or exported.
This field is disabled if the designated Action is either View or
Delete.

Event Handler Parameters - Edit Collection Action…
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of any of the Collection… events.
The fields and options specific to this response type include these:
Field/Option

Description

Collection

Use the drop-down list to select which collection is to become the
object of the specified action.

Infor Mongoose Application and Form Development | 1353

WebForms-NoTOC

Field/Option

Description

Navigation Action

Note: This option is available only with the Collection Navigate
response type.
Use this drop-down list to specify how the system is to navigate
when this event is generated. Options include:
•
•
•
•

First - (Default) Go to the first record in the collection.
Next - Go to the next record in the collection.
Previous - Go to the previous record in the collection.
Last - Go to the last record in the collection.

Edit the Command to Execute
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Execute EXE or other program.
The fields and options specific to this response type include these:
Field/Option

Description

Command

This option specifies a command string that opens an application,
a document, or a URL (web page). Enter the command as you
would enter any Run command from the Windows Start menu.
To open a document associated with an application, enter the full
(absolute) document path. You do not need to specify the application path.

Event Handler Parameters - Edit Script Name and Parameters
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Form Script Method or Run Script.
Use this form to select the name of a global script that you want to use. Scripts that are stored as global
objects can be used in these objects:
Field/Option

Description

Name

This field specifies the name of the global script. This name must
be unique in the forms database.

Parameters

This field specifies the parameters of the script displayed as a
comma-delimited string.
To edit the script, click Edit.

Infor Mongoose Application and Form Development | 1354

WebForms-NoTOC

Event Handler Parameters - Edit Generate Application Event
Parameters
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Generate Application Event.
Field/Option

Description

Event

This required field specifies the name of an application event defined in the Events form.
Only custom, non-framework events can be specified.

Synchronous

With this option is selected, form processing is suspended until
the Application Event System executes its event handlers.
Output values returned by handlers for the application event can
be processed into form components, variables, or properties, as
specified in parameter Value settings. Any updated components
and properties are marked as modified.
If you clear this check box, no processing of output parameters
occurs in the form. The form continues to execute after the event
is generated, allowing the application event system to operate in
parallel with Windows client.

Name

This field specifies the name of a parameter defined for the event
in the Application Event System.
To return the result of a synchronous event to the form component,
variable, or property specified in the Value field, enter the keyword
RESULT.

Value

This field specifies the value to be used for the parameter.
Windows client substitution keywords and literals are supported
as parameter values.
To add or edit a literal value or a component, variable, or property
value, click Edit Parameter.
If Synchronous is selected and a parameter with the same name
is defined as an output parameter in an application event handler,
output values can update form components, variables, or properties. To enable updates of this type, use the substitution keywords
C(ComponentName), V(VariableName), or P(PropertyName) as
values. Event handler output from such parameters is placed in
the named components, variables, or properties.

New Parameter

Click to create a new row in the Parameters grid.
To specify a new parameter, enter a Name, and then enter a
Value or click Edit Parameter to specify a Value.

Delete Parameter

Click to delete the current row in the Parameters grid.

Edit Parameter

Click to edit the Value in the current row in the Parameters grid.

Infor Mongoose Application and Form Development | 1355

WebForms-NoTOC

Event Handler Parameters - Edit Event to Generate
This version of the Event Handler Parameters form is used with form events that have been assigned
a Response > Type of Generate Event.
Field/Option

Description

Event

The name of the standard event or custom event to be generated
by the event handler.

Post event asynchronously

To have the system generate the event handler after the current
