Reference

So, rather than delete handlerS1, Infor marks it as Obsolete before the next release. Then, when the
update is installed, the reference to handler S1 is not broken, though the system ignores it when
handling this application event, as in this diagram:

Infor Mongoose Application and Form Development | 847

Reference

Infor Mongoose Application and Form Development | 848

Mongoose Glossary

Mongoose Glossary

Alias
A name assigned to a virtual directory in a Web site.

All Forms folder
In the Windows client Master Explorer folder, the All Forms folder is a read-only folder that lists each
unique form name residing in the forms database, including any new forms created at a customer site.
The form titles displayed reflect the Vendor default versions of the forms. All users can display the All
Forms folder and can copy forms from it. No user can delete or otherwise change the forms listed.
When a user opens a form in All Forms, the version displayed is determined by the user's ID and by
the hierarchy of user-group-site versions that exist in the system.

Application database
A database that contains the primary data maintained by users of an application. In an inventory
application, for example, the database might contain product numbers and quantities. By default, an
application database contains stored procedures supporting WinStudio functionality, user-defined data
types, and tables for storage of administrative information, such as user names and passwords.

Archive database
A database used by FormControl during the development of a WinStudio application. FormControl
uses an archive forms database and an archive templates database to store definitions of forms and
form-related objects that have been deleted from the FormControl production databases.

Infor Mongoose Application and Form Development | 849

Mongoose Glossary

Property (forms and components)
A property set defines a WinStudio form or component. For a form, properties include the form caption
and the standard operations available to the user. For a component, properties include whether the
component is read-only or hidden, what data is bound to the component, and so forth. When a form
or component is customized, its properties are changed.

AutoRun folder
A folder in WinStudio's My Folders that lists forms to be opened automatically when WinStudio is
opened. If more than one form is listed, the opened forms are cascaded in the order in which they are
listed in the AutoRun folder. Users with site developer editing permission cannot see the contents of
a user's AutoRun folder.

Background task
In WinStudio, a background task is a stored procedure, a report, or an executable program that is
executed by Infor ERP TaskMan on page 863. A background task can be run on demand from a
WinStudio form, or it can be run on a schedule maintained by the Microsoft SQL Server Server Agent
utility.

Basic editing permissions
In WinStudio, a level of editing permission that allows users to make limited customizations to forms.
Users can access WinStudio's Design mode, make customizations to existing forms, and save
customizations as their own user version of a form. Users cannot create new forms or delete existing
ones (other than their user version). In the Explorer, a basic user has the same capabilities as a user
with no editing permission. Allowed customizations to forms include the following:
•
•
•
•

Resize a form
Change a form's caption
Change the tab order of components on a form
Edit components in minor ways, including resizing, moving, and hiding components; changing
default values; changing component labels; and setting upper case or read-only properties.

Infor Mongoose Application and Form Development | 850

Mongoose Glossary

BLOB data
BLOB (binary large object) data in SQL Server databases support images, sounds, multimedia, and
other application file types. To employ BLOB data in an application, define a table column in the
application database that has Image data type. Create an IDO property with Binary data type that is
associated with the Image column. In Windows client, create a static or button component that is bound
to the IDO property (and inherits its data type). End users can use the Edit menu to view, import, export,
and delete BLOB data associated with the component.

Bound property
A property in an IDO that is bound to a column in a table in the application database.

Caption
The title of a WinStudio form displayed in the title bar of the open form window and in the title bar of
the application window. The form caption differs from the form's internal name (which is used in the
forms database) and may differ from its displayed title in the Explorer. A component caption is the label
associated with a component on a form. Like a form caption, a component caption is distinct from a
component's internal name.

Check box component
A WinStudio form component used to display and manipulate yes/no or true/false types of responses.
By default, the value of a selected check box is 1 and the value of a cleared check box is 0. End users
click a check box to set its value and can select more than one check box in a group.

Child form
A WinStudio form accessed from a parent form that depends on information passed from the parent
form.

Infor Mongoose Application and Form Development | 851

Mongoose Glossary

Collection
The result set returned by an IDO. The result set is typically a group of related data items that displays
in a form. Most forms have only one collection, the primary collection. Other forms have multiple
collections. A collection can include a subcollection.

Collection-level commit
As a WinStudio user inserts, deletes, or modifies items in an IDO collection, changes are not saved
and committed to the application database until the user closes the form or clicks the collection save
command. At that time, all items in the collection are committed. At any time, a user can save changes
to the collection or to an individual item in the collection by using commands on the Actions menu.

Combo box component - defined
A Windows client form component that allows data to be typed in an edit box or selected from a list of
values.

Component
One of the visible elements on a WinStudio form that support the presentation and manipulation of
data. Components have properties such as data binding, formatting, data types, default values, validation,
and the generation of events. Types of components include static box, edit box, multiline edit box, list
box, drop-down list box, combo box, date combo box, push button, check box, radio button, notebook
and notebook tab, grid and grid column, graph, form page, and group box.

Component class
In Windows client, a type of global object that packages sets of component properties. A component
class allows you to reuse specifications for a component on multiple forms. For example, instead of
specifying a caption, a list of values, and a validator for a component on each form, you can create a
component class that bundles the properties. You can then base a component on the component class
to inherit those attributes automatically.

Infor Mongoose Application and Form Development | 852

Mongoose Glossary

Configuration
A set of references, specified in Configuration Manager, to databases in which the framework stores
development and run-time information. A configuration encapsulates server and database names,
logon information, tool-specific options, and project paths. A configuration name is required to open
WinStudio, FormSync, and FormControl.

Configuration Manager
A utility used to create and edit framework configurations. Configuration Manager can build SQL Server
and Oracle databases used in configurations.

Container
Certain WinStudio components are designed to function as containers for other components. These
components include:
•
•
•
•
•
•
•
•

Notebooks, which serve as containers for notebook tabs
Notebook tabs, which in turn serve as containers for other components that display only on that
tab
Grids, which serve as containers for grid columns and/or tile forms
Vertical grids, which serve as containers for vertical grid elements
Group boxes, which are used to group and frame a group of other related components
Form pages, which serve as containers for any number of other components in a multi-page form
FlexLayouts, which can contain virtually any other type of component and are designed to provide
the ability to set up a flexible and dynamically responsive layout for the form
Infocons, which serve as more flexible containers for tile forms, allowing for more options tp display
tile form data

Current collection
In WinStudio, the IDO collection that is active and can be edited, saved, or otherwise altered by the
user. If a form displays more than one collection, a user chooses the current collection by clicking in
it.

Infor Mongoose Application and Form Development | 853

Mongoose Glossary

Current IDO item
In WinStudio, the item in an IDO collection that is active and can be edited, saved, or otherwise altered
by the user. The user selects the current item by clicking in it.

Data type
In WinStudio, a component property that determines how to validate and present data in the component.
Each component defaults to string data with an unlimited number of characters. Components may be
bound to IDO properties, in which case the data type is determined by the IDO.

Date combo component - defined
A Windows client form component that is a special kind of combo box component for entering dates.
When the drop-down button is pressed, a calendar is displayed for selecting a date.

Derived property
An IDO property whose value is derived from an SQL expression. Derived properties are used to
calculate values, to execute subqueries, or to call SQL functions.

Derived value
In Windows client, the value of a component whose data binding consists of substitution keywords,
literal values, or a combination of both. For example, an edit box with the data-binding specification
Customer number P(CustomerNum) displays the literal string Customer number followed by the
value of the CustomerNum property of the current IDO item. The component is not bound to an external
data source.

Development forms database
A database that stores specifications for forms and form-related objects created or edited in WinStudio
Design mode. When development and testing is complete, the objects are copied to the deployed forms

Infor Mongoose Application and Form Development | 854

Mongoose Glossary
database. If you use FormControl to maintain version control in the forms database, the development
forms database accessed in WinStudio Design mode is also accessed by FormControl. FormControl
checks objects out of the FormControl production forms database and into the development forms
database.

Development templates database
A SQL Server database that stores in-process specifications for form templates created or edited in
WinStudio design mode. When development and testing is complete, the objects are copied to the
deployed templates database. If you use FormControl to maintain version control in the forms database,
the development templates database accessed by WinStudio design mode is also accessed by
FormControl. FormControl checks objects out of the FormControl production templates database and
into the development templates database.

Diagnostics
A WinStudio function that lets you examine the programming calls and other actions triggered by a
user's activity.

Drop-down list component - defined
A Windows client form component that allows data to be selected from a list of values. A drop-down
list always displays its current value, but it only displays its list when the user clicks the down arrow.

Editing permissions
Capabilities in WinStudio that control whether a user can access Design Mode to create or customize
forms. In the WinStudio Explorer, editing permissions also determine whether developers can set up
the Master Explorer folder or customize public and user folders.
Developers creating an application have Vendor developer editing permissions on page 875, which are
set by Configuration Manager.
Developers customizing a deployed application have basic editing permissions on page 850, full user
editing permissions on page 859, or site developer editing permissions on page 871, which are assigned
by the system administrator. Users can be assigned no editing permissions on page 865.

Infor Mongoose Application and Form Development | 855

Mongoose Glossary

Editing scope
In WinStudio, the interaction between a developer's editing permission and his or her ability to save
forms and global objects at a given level of the user-group-site hierarchy.
•
•

To create an application, developers require Vendor developer editing permissions in order to
create Vendor default versions of forms and global objects in the forms database.
To customize a deployed application, developers require basic, full user, or site developer editing
permissions in order to create user, group, or site versions of forms and global objects.

Edit component
A WinStudio form component used for the display and standard keyboard input of text. An edit
component can be bound to individual properties or variables. The value of the property or variable is
displayed and edited in the component's edit box. The label for an edit component is often supplied by
a static component to the left of the edit component.

Design mode
A way of accessing WinStudio that allows you to create or change forms, components, and global
objects. You can toggle between Design mode and Run mode.

Enhanced combo box component
A WinStudio form component that allows the user to type text directly into the component or to select
from a drop-down list of values. Like the combo box, the enhanced combo box only displays the list
when the down arrow is clicked. Otherwise, it displays the currently selected item in the edit portion of
the control. Compared to combo boxes, enhanced combo boxes perform better in grids and are
adjustable in height.

enum value
An enum value is an internal mechanism used to control the order in which items in a drop down list
or combo box are displayed (assuming an alphanumeric order is not desired, for some reason). This
value can also be used to perform SQL queries.

Infor Mongoose Application and Form Development | 856

Mongoose Glossary

Event handler
In WinStudio, an object that is specific to a particular form and that supplements, alters, or cancels the
default event-handling behavior of WinStudio. The developer creates and names event handlers. An
event handler executes when an event matching the name of the event handler is generated. The
event can be generated by WinStudio as the user interacts with a form, or it can be generated
programmatically by the developer.

Explorer
In WinStudio, the window that lists forms in a tree hierarchy similar to the Windows Explorer.

Filter
In WinStudio, criteria that identify which items should be retrieved in an IDO collection from the
application database. For example, a company address may contain a company name, street address,
city, state, and postal code. If the postal code 43222 is used as a filter, only items sharing that code
are retrieved in the collection.

Filter-in-Place
In WinStudio, a way of using a form to specify which items to retrieve from the application database
as the current IDO collection. When Filter-in-Place is active, users enter values in components as filter
criteria that specify items to be retrieved.

Focus
In WinStudio, the action of making an IDO collection or a particular item in the collection the currently
active collection or item that can be edited, saved, or otherwise altered. Typically, a user changes focus
by clicking the desired collection or item.

Infor Mongoose Application and Form Development | 857

Mongoose Glossary

FormControl
A utility used in the development of WinStudio applications. FormControl maintains version control for
forms and global objects stored in the forms and templates databases. Developers with Vendor editing
permissions in WinStudio can use FormControl to check forms and global objects out of FormControl
production databases and into the development databases used by WinStudio. FormControl tracks
only the Vendor default version of objects.

FormSync
A utility used with a deployed Windows client application that has been customized. When a site is
upgraded to a new version of a Windows client application, FormSync can be used to retain and upgrade
customizations made in the previous version of the application. FormSync can also be used to copy
customized objects from one forms database to another. FormSync works only with customized client-tier
objects (user, group, or site versions of forms and global objects) in the Windows client forms database
and does not affect the application database, the templates database, the objects database, or middle-tier
code.
Most FormSync operations can also be performed using the FormSync form.

Forms database
A database that stores specifications for forms and form components, including validation procedures,
scripts, variables, and other objects. Windows client retrieves and interprets the specifications at run
time to create a form. The default forms database includes definitions of administrative forms, such as
the Users form.
If you are customizing a Windows client application, we recommend that you maintain a development
forms database that is accessed by developers using Design mode. This development database stores
product changes until they are tested and ready to be copied to the deployed forms database, which
is accessed by end users.

Form
The application windows created and displayed in WinStudio. Users use forms to enter application
data, to search for information, or to run processes and reports.

Infor Mongoose Application and Form Development | 858

Mongoose Glossary

Form name
In WinStudio, the internal name of a form used in the forms database, as opposed to its caption, which
is displayed when the form is opened, and its title, which is listed in the Explorer. Each form name is
unique in the forms database.

Form page component - defined
A Windows client form component that is an individual page of a form. Usually, form pages are used
to implement wizard-style user interfaces.

Form title
In WinStudio, the title of a form listed in the Explorer window, as opposed to its caption, which is
displayed when the form is opened, and its internal name, which identifies the form in the forms database.
All users can customize the form titles that are used in their My Folders folder.

Full user editing permissions
In WinStudio, a level of editing permission that allows form customization in a deployed application.
Full user editing permissions allow a developer to enter Design mode, make any type of change to an
existing form, and save customizations as his or her user version of the form. Full users cannot create
a form or delete a form (other than their user version). In the Explorer, a full user has the same
capabilities as a user with no editing permissions.

Global objects
In WinStudio, global objects are component classes, property class extensions, variables, validators,
strings, scripts, and menus whose operation affects more than one form. Like forms, global objects
can be customized so that different versions of them exist in the forms database. Global objects are
loaded into memory each time a form that references them is opened. They stay in memory until you
unload them or exit WinStudio. Developers with site developer editing permissions may need to unload
global objects if they use impersonation to change their editing scope. Otherwise, the correct version
of a form or global object may not be accessed.

Infor Mongoose Application and Form Development | 859

Mongoose Glossary

Workspace
A configuration of forms that open together. If you use a group of forms often, you can set up a named
workspace that lets you open all the forms with one command, with the size and position of the forms
preset.

Graph component - defined
A special Windows client form component used to render business graphs of IDO collection data.

Grid column component - defined
A Windows client form component that is an individual column in a grid component. The grid column
is contained within the grid.

Grid component - defined
A Windows client form component that allows data to be displayed and maintained in a tabular
(row-column) format.

Grid form
In WinStudio, a type of form that uses a row-column table to display a collection.

Group box component
A WinStudio form component that is a labeled box used to frame a group of components. Group boxes
are visual devices that help users understand how several components are related in function. Group
boxes display a label and an outline; they have no data binding and are read-only.

Infor Mongoose Application and Form Development | 860

Mongoose Glossary

Group version
In Windows client, a form or global object customized with Group scope. The version is associated
with the name of an administrative group defined in the Groups form. The version is accessed by all
users in the named group, unless it is superseded by a user version. The version can be customized
by a user with Site Developer editing permission.

GUID
A SQL Server uniqueidentifier column used in application database tables. A GUID is a 128-bit number
generated by a call to Windows that is statistically guaranteed to be unique. IDOs require application
database tables to include a RowPointer column that uniquely identifies a row in the table. GUID is the
recommended data type for the RowPointer column.

IDO (intelligent data object)
A component that implements a set of methods and properties used by clients to perform such tasks
as loading and updating collections or retrieving information about data. Related IDOs are typically
grouped into an IDO project. Together with the IDO runtime service, IDOs are deployed as the middle
tier of an application.

IDO project - defined
A group of one or more IDOs (intelligent data objects).

Impersonation
In WinStudio, the ability of users with site developer editing permissions to change their editing scope
to create user versions, group versions, or site versions of a form or global object. When site developers
change their scope to create user or group versions, they identify (or impersonate) which user or group
is affected.

Infor Mongoose Application and Form Development | 861

Mongoose Glossary

Inheritance of property attributes
WinStudio components that are bound to an IDO property inherit (or override) the attributes of the IDO
property. Additionally, an IDO property can inherit (or override) attributes from an IDO property class,
and WinStudio components can inherit (or override) attributes from component classes and property
class extensions.
The inheritance hierarchy for components bound to IDO properties is as follows:
IDO property class (if any)
IDO property
WinStudio property class extension (if any)
WinStudio component class (if any)
WinStudio component
For components not bound to IDO properties, the inheritance hierarchy is:
WinStudio component class (if any)
WinStudio component

Inline list
In WinStudio, a list of values in which the values are stored in the component itself. In other types of
lists, values may be from a collection, may be retrieved by a method, or may be defined by a script.

Key
A unique identifier for a row in an SQL table in an application database.

Linked form
In WinStudio, a form linked to another form through a button.

List box component - defined
A Windows client form component that allows data to be selected from a list of values. A list box always
displays a scrollable list.

Infor Mongoose Application and Form Development | 862

Mongoose Glossary

List of values
In WinStudio, a list of possible choices from which a user selects a value. The following component
types present a list of values: list box, combo box, enhanced combo box, drop-down list, grid column
component.

List source
In WinStudio, the source of values for a list box, drop-down list, or other list-type component. The
specification can identify an IDO collection, an IDO method, an inline list source, or a script.

localhost
A placeholder in a URL for the name of the computer on which a Web server is running.

Infor Framework TaskMan
Infor Framework TaskMan runs on the Application Server as a Windows service and monitors the
application database for background tasks, executes them, and returns results.

Master Explorer folder
In WinStudio, the default folder in the Explorer window. The Master Explorer folder includes the read-only
All Forms folder and any other folders created for an application. Only users with Vendor developer
editing permissions can modify the Master Explorer folder.

Menu
In WinStudio, a global object consisting of one or more shortcut menu items. Each menu item generates
one event.

Infor Mongoose Application and Form Development | 863

Mongoose Glossary

IDO method
A method that can be executed through a call to an IDO. Some methods are standard and common
to all IDOs, such as load collection. Others are implemented as custom stored procedures in the
application database or as custom VB.NET code in IDO extension classes.

Modal form
In WinStudio, a child form accessed from a parent form that passes some information to the child form.
The user must close a modal form in order to return to the parent form.

Multiline edit component - defined
A Windows client form component used for the display and input of large blocks of text. A multiline edit
component can be bound to individual properties or to variables. The value of the property or variable
is displayed and edited in the component's edit box. The component supports word wrapping and
vertical scrolling.

Multiview form
In WinStudio, a form with a vertical split bar that divides the form into two panes. One pane is a grid
displaying all the items in the current collection. The other pane displays a single item in the collection.

My Folders
In the WinStudio Explorer, users can display and modify the forms and folders listed in their own My
Folders folder. Only users with site developer editing permissions can see and modify the My Folders
folders of other users (except the AutoRun and PreLoad folders).

Infor Mongoose Application and Form Development | 864

Mongoose Glossary

Notebook component - defined
A Windows client form component that allows other components to be grouped together on notebook
tab components. Only the components on the currently selected tab are visible to the user. Notebooks
are useful for grouping components and managing screen space.

Notebook tab component - defined
A Windows client form component that is an individual tab of a notebook.

Note
In WinStudio, a note is a comment that a user attaches to an item in an IDO collection or to an entire
IDO collection. A note can be text that is entered or an external file that is attached. External files can
be text documents, spreadsheets, graphics, or multimedia.

No editing permission
In WinStudio, a level of editing permissions that allows users to operate only in Run mode and excludes
them from Design mode. All menu commands related to Design mode are disabled for users with no
editing permission. In the Explorer, a user with no editing permissions can display the Master Explorer
folder, all public folders, and can customize his or her My Folders.

Objects database
A SQL Server database that stores the definitions of IDOs created during application development.

Actions menu
A Windows client menu that is specific to a particular IDO collection on a form. The menu is always
the second menu on the menu bar when a form is open. The displayed name of the menu is specified
in Configuration Manager. By default, the name is "Actions." The name can be set to a literal or string
value such as "Objects," which displays when any form is open; or the name can be set so that it

Infor Mongoose Application and Form Development | 865

Mongoose Glossary
changes dynamically to match the caption of the current form. The shortcut menu that opens when a
user right-clicks a form contains the same commands as the Actions menu.

Actions menu item component
A Windows client form component that is an option on the collection's Actions menu.

Optional field
In WinStudio, a component that can be completed or left blank; an item in the IDO collection can be
saved even if the component is blank.

Parent form
In WinStudio, a form that passes information to a child form.

Permanent filter
In WinStudio, a filter that functions automatically when the form is opened. A permanent filter is specified
in the form's collection definition.

PreLoad folder
In WinStudio's Explorer, a folder in My Folders that lists forms to be opened quickly. When a user logs
on, these forms are retrieved from the forms database and kept in memory so they will display more
quickly when they are opened. Developers with site developer editing permissions cannot see the
contents of a user's PreLoad folder.

Infor Mongoose Application and Form Development | 866

Mongoose Glossary

Primary collection
The IDO collection associated with and bound to a form at the time of the form's creation
•
•

If a form has only one IDO collection associated with it, that collection is, by definition, the primary
collection.
If a form has more than one IDO collection associated with it (such as a subcollection or secondary
collection), the primary collection is considered the top-level IDO collection on the form.

There can be only one primary collection per form.

Production forms database
The version of a forms database that is deployed with a WinStudio application and accessed by end
users.
In WinStudio development with FormControl, a database used by FormControl to store the current
master version of forms and global objects that are created or edited in WinStudio Design mode.
FormControl checks objects out of the production forms database and into the development forms
database, where they are edited in WinStudio.

Production templates database
The version of a templates database that is deployed with a WinStudio application and accessed by
developers customizing the application.
In WinStudio development with FormControl, a database used by FormControl to store the current
master version of form templates that are created or edited in WinStudio Design mode. FormControl
checks objects out of the FormControl production templates database and into the development
templates database, where they are edited in WinStudio.

IDO Property
IDO properties usually correspond to columns in a database table. Properties are related pieces of
information in an item in an IDO collection. For example, a company-address item includes related
properties such as the company name, street address, city, state, or postal code. IDO properties include
bound properties, derived properties, subcollection properties, and unbound properties .

Infor Mongoose Application and Form Development | 867

Mongoose Glossary

Property class extension
In WinStudio, a type of global object that overrides the attributes of an IDO property class of the same
name. Property class extensions apply only to components that are bound to IDO properties for which
there is an IDO property class. A WinStudio property class extension lets you manipulate the attributes
of all components bound to the IDO properties that inherit attributes from the same IDO property class.

Public folder
A folder in the WinStudio Explorer that is created in a deployed application and is available to all users
of the application. Only developers with site developer editing permissions can create public folders.
Developers with Vendor default editing permissions do not see public folders.

Push button component
A WinStudio form component that is a standard Windows command button. Typically, it is used to
initiate an action such as running a procedure or launching another form.

Query form
In WinStudio, a special form used to search the database for items that match specified criteria. The
search criteria users enter in a query form act as a filter attached to the current collection. The filter
can be saved and maintained through the query form. A saved filter can be used from any form that
displays the same collection.

Radio button component
A WinStudio form component used to select from a set of mutually exclusive options. Each button
represents one of the options. When an option is selected, any previously selected radio button is
automatically cleared.

Infor Mongoose Application and Form Development | 868

Mongoose Glossary

Required field
In Windows client, a required field is a component that must be assigned a value before the current
IDO item can be saved, an indicator is shown for required fields. You can customize the required field
indicator using the Theme Editor.

Reusable note
In WinStudio, a type of note that is written once but can be attached multiple times to different items
and collections. If a reusable note is marked as a system note, it can be read and attached by all users.

Reversion
In WinStudio, the process of deleting one version of a form or global object and going back to the next
existing version in the user-group-site hierarchy. Specifically, a user version reverts to a group version.
A group version reverts to a site version. A site version reverts to a Vendor default version.

Right-click menu
In Windows client, a menu associated with a form or a component that is displayed when the user
right-clicks the form or the component. On a form, the Actions menu for the current collection is
displayed. On a component, the shortcut menu created for the component is displayed.

Run mode
The WinStudio mode in which end users view, enter, insert, and otherwise interact with data. Users
with editing permissions can toggle between run mode and Design mode.

sa login
The SQL Server sa (system administrator) login is a built-in login assigned to the sysadmin fixed server
role. The sa password for a server is required to perform certain tasks in Configuration Manager.

Infor Mongoose Application and Form Development | 869

Mongoose Glossary

Scope
The system identifies versions of a form by a combination of form name, scope type, and scope name.
Scope type specifies whether a customized form is to be used by a single user (User scope type), a
set of users (Group scope type), or by all users (Site Default scope type). Scope type is qualified with
a scope name. User scope is qualified with a user name. Group scope is qualified with the name of an
administrative group defined in the Groups form.
Developers with Site Developer editing permissions specify scope type and scope name when they
customize forms. Windows client sets scope type and scope name automatically when users with
lower-level editing permission customize forms.
A default form supplied by an application vendor has Vendor scope type.

Scripts
In WinStudio, scripts can encapsulate VB.NET code. Scripts can be global objects stored in the forms
database and referenced by more than one object. These global scripts are used by validators or event
handlers of the Run Script type. Other scripts are specific to a validator or an event handler of the Inline
Script type and cannot be referenced by other objects. Form scripts are specific to a form and cannot
be referenced by other objects.

Secondary collection
An additional IDO collection associated with and bound to a form.
A secondary collection might or might not be directly related to the primary collection on a form. With
the proper settings, a secondary collection can be used as a subcollection.

Shortcut menu
In WinStudio, a type of global object that is associated with one or more components and consists of
one or more menu items. Each menu item generates one event. A shortcut menu is displayed when
the user right-clicks the component.
A form's shortcut menu is not a global object, but a list of commands that execute standard operations
against the current collection.

Infor Mongoose Application and Form Development | 870

Mongoose Glossary

Site developer editing permissions
Site developer editing permissions allow developers to enter Design mode and make any type of change
to forms, including creating new forms, and modifying or deleting existing ones. Site developers can
save customizations as a user version for any user, as a group version for any group, or as a site
version. Site developer editing permissions allow developers to use FormControl. In the Explorer, only
site developers can create and modify public and user folders.
Note: In WinStudio, site developers can create and modify data at the User and Group level, as well
as the Site Developer level. In FormControl, however, site developers can create and modify data only
at the Site Developer (or Vendor Developer) level. FormControl does not provide for development at
the User or Group level.

Site-default version
In WinStudio, a form or global object customized in a deployed application by a developer with site
developer editing permission. It is accessed by all users at a site, unless it is superseded by a group
or a user version. It is customized with editing scope set to Site Default.

Static component
A WinStudio form component used to present read-only text and graphics on a form. Generally, a static
component displays the value of a text string rather than literal text. Static components are often paired
with edit boxes or list boxes and function as the label for the other component.

String
In Windows client, a type of global object that encapsulates a value, usually a set of alphanumeric
characters. Using strings rather than literal text as form or component captions supports consistency
and eases customization. For example, changing the value of the string sPartNo from Part No. to
Part Number changes the value in all forms and components that use the string. Strings are stored
in a strings table in the forms database, which can contain multiple versions of the strings table. One
version of the strings table is current, as determined by the language setting in Windows.

Infor Mongoose Application and Form Development | 871

Mongoose Glossary

Subcollection
A 'child' IDO collection for which data is specified by filtering a 'parent' IDO, known as the primary
collection.
A subcollection's items are associated with and dependent on items in the primary collection. For
example, if the primary collection on a form contains invoice records, a subcollection on the same form
might display individual line items associated with a particular invoice item.

Subcollection property
An IDO property that specifies a child IDO that is filtered from a parent IDO. Use this type of property
for dependent, child relationships, such as those involving header data items and detail data items.

Substitution keyword
In WinStudio, a type of keyword that returns certain system or form values. For example, the substitution
keyword USERNAME() returns the name of the current user.

Super user
In WinStudio, a user with permission to access all forms and execute all form operations. The system
administrator assigns this ability when a user is registered. Alternatively, the administrator can assign
a user or a group access to only certain forms and certain operations.

System note
In WinStudio, a type of reusable note that can be read and attached by all users. A reusable note that
is not marked as a system note can be read only by its author.

Templates database
A database that stores specifications for form templates defined in WinStudio Design mode. Users of
a deployed application do not access the database.

Infor Mongoose Application and Form Development | 872

Mongoose Glossary

Tools login
A user ID and password specified for developers in Configuration Manager. In WinStudio, the login
has Vendor Developer editing permission and Superuser access (the ability to access all forms and
execute all form operations). FormControl refers internally to the user ID in tracking check-ins and
check-outs.

Unbound property
An IDO property that is not bound to a column in a table and is not persistent. Use this type of property
to pass data from a form to an IDO when the IDO is not bound to a column. The data can be used by
a customized insert, update, or delete method.

User-defined property
In a deployed application, a custom property attached to an existing collection in the application
database. For example, assume that an application has a Contacts collection that includes properties
such as name, department, title, and telephone number. A developer customizing the application can
add a new property for a nickname by creating a user-defined property for the Contacts collection. The
developer can also create a user-defined type to provide values for the new nickname property.

User-defined type
A user-defined type specifies values for a user-defined property. For example, if a user creates a
user-defined property for color, the user could also create a user-defined type that specifies the values
red, blue, and green. The user-defined type supplies a valid list of values for the property.

User folders
Folders in the WinStudio Explorer that are created automatically by WinStudio. User folders mirror
each user's My Folders folder. User folders are displayed only to users with site developer editing
permissions. Site developers can also edit user folders. Any changes are reflected in the user's My
Folder folder, but the changes are not secure because users can modify their My Folders folder.

Infor Mongoose Application and Form Development | 873

Mongoose Glossary

User-group-site hierarchy
In WinStudio, if more than one version of a form or global object exists in a deployed forms database,
the user-group-site hierarchy determines which version is displayed. This hierarchy also determines
how one version reverts to another version.
•
•
•
•

User version. Displayed to only one user. If it exists, a user version is always displayed and overrides
any other version that exists.
Group version. Displayed to only the users in one group. If no user version exists for a user in the
group, the group version is displayed and overrides the site or Vendor default version.
Site version. Displayed to all users at a site if no user or group version exists for a user. Overrides
the Vendor default version.
Vendor default version. Displayed to all users at a site. If no user or group version exists for a user
and if no site version exists, the Vendor default version is displayed.

User permissions
In WinStudio, capabilities set up by the system administrator that control which forms a user can display
and whether the user can insert, modify, delete, or only display information.

User-specified filter
In WinStudio, filter criteria that users enter through Filter-in-Place or a query form. The filter specifies
which records should be retrieved from the application database.

User version
In WinStudio, a form or global object customized with scope set to User. The version is associated
with one user name in the system, and it is accessible only by that user. The version can be customized
by the named user with Basic editing permission or Full User editing permission. It can also be
customized by a user with Site Developer editing permission; in this case, the developer sets scope
to User and specifies the user name.

Validator
In WinStudio, a type of global object that defines specific conditions for determining whether input to
a component is valid. For example, a validator can check that the user enters values between 100 and

Infor Mongoose Application and Form Development | 874

Mongoose Glossary
200. Besides checking for valid input, validators can output other data to the form. You can create and
attach error messages to validators.

Validator output
In WinStudio, data that is returned to a form through a validator. For example, a validator for a component
may verify a company code and, if the entered code is valid, return a company name into another IDO
property on the form. This output causes the company name to be displayed by another component
on the form that is bound to the company name property.

Variable
In WinStudio, a variable is either specific to one form or is a global object used by multiple forms. You
can bind a variable to a component. You can use variables to control the initial action of a form.

Vendor version
In WinStudio, the version of a form or global object developed with Vendor editing permission. This
level of editing permission is normally available only to suppliers of application software; it is not available
to users who customize forms supplied by an application vendor. Vendor versions provide the default
functionality of an application. The Vendor version of an object is not changed or deleted in WinStudio
Design mode with normal editing permission (Basic, Full User, or Site Developer). In a vendor-supplied
application upgrade or service pack, Vendor versions may be deleted and replaced in a forms database.

Vendor developer editing permissions
In Windows client, the level of editing permissions required to create an application. Objects edited
under Vendor Developer editing permissions are saved as Vendor default versions.
Permissions are assigned on the Users form. Vendor developer editing permissions allow developers
to use FormControl. Vendor developers can make any type of change to an existing form and can
create and delete forms. Vendor developers see only the Vendor default versions of forms and do not
see user or group versions. In the Explorer, only Vendor developers can modify folders in the Master
Explorer. Vendor developers do not see public or user folders. A Vendor developer has the same
capabilities as an end user to view and modify folders in My Folders.

Infor Mongoose Application and Form Development | 875

Mongoose Glossary

Virtual directory
A directory that is not physically contained in the home directory of a Web site served by Internet
Information Server or Personal Web Server but appears to client browsers as though it were. A virtual
directory is referred to by an assigned alias rather than a path.

Wildcard character
In WinStudio, a symbol used to represent any character in a query or search. For example, a* matches
any value that begins with a, regardless of how many characters follow it.

WinStudio
WinStudio is both the user interface for end user access to an application and the development interface
for creating forms and constructing the Explorer tree that provides access to forms.

Workspace
A configuration of forms that open together. If you use a group of forms often, you can set up a named
workspace that lets you open all the forms with one command, with the size and position of the forms
preset.

Infor Mongoose Application and Form Development | 876

Field Help-NoTOC

Field Help-NoTOC

Read Mode and isolation levels
The Read Mode field controls the isolation level for transactions when loading a collection, permitting
or preventing dirty reads.
The setting UNCOMMITTED implements dirty reads, in which no shared locks are issued and no
exclusive locks are honored. It is possible to read uncommitted data; values can be changed and rows
can appear or disappear in the collection before the end of the transaction. The setting corresponds
to the Transact-SQL statement SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED.
The setting COMMITTED specifies that shared locks are held while data is being read to avoid dirty
reads, but the data can be changed before the end of the transaction. The setting corresponds to the
Transact-SQL statement SET TRANSACTION ISOLATION LEVEL READ COMMITTED.
The Read Mode field overrides the default value for Collection Read Mode set in the core form Process
Defaults.

Infor Mongoose Application and Form Development | 877

Dialogs-NoTOC

Dialogs-NoTOC

Add IDO Project dialog box
Use this dialog box to create an IDO project "on the fly" from the New Data Maintenance Wizard.
This is useful when you have started to create a new data maintenance table, IDO, and form set, but
you do not want to use an existing IDO project.
This dialog box is available only from the New Data Maintenance Wizard.

Add Property Class dialog box
Use this dialog box to create a property class from within the New Data Maintenance Wizard (NDMW).
Normally, a property class is created before applying it within the NDMW. However, in cases where
the property class has not been created ahead of time, this dialog box allows you to create one without
having to exit the NDMW, create the property class, and then start the NDMW all over again.
This dialog box offers only the basic options for a property class. If you want a more refined property
class, you must later use the Property Classes form to make your changes.
The Add Property Class dialog box is accessed by clicking the Add option for the Property Class
column in the New Data Maintenance Wizard.

Add User Defined Type dialog box
Use this dialog box to create a SQL user-defined data type (UDT) from within the New Data Maintenance
Wizard (NDMW).
Often, a UDT is created before applying it within the NDMW. However, in cases where the UDT has
not been created ahead of time, this dialog box allows you to create one without having to exit the
NDMW, create the UDT, and then start the NDMW all over again.
The Add User Defined Type dialog box is accessed by clicking the Add option for the SQL Data Type
column in the New Data Maintenance Wizard.

Infor Mongoose Application and Form Development | 878

Dialogs-NoTOC

Additional Graph Settings dialog box
Note: This topic applies to the graphing tool, which is supported for backward compatibility. We
recommend that, if you want to add charts or gauges to a form, use the newer FusionCharts tool instead.
See About charts and gauges on page 183.
These tables list and describe the settings and options available in this dialog box.
Layout options
These options pertain to the Layout group.
Field/Option

Description

Type

This option sets the relative position of the chart, title, and legend within a graph
component.
For example, to position the graph on the left, the graph title on top, and the
legend on the right, select Chart - Left / Title - Top / Legend - Right.
To specify custom positions, sizes, and margins for the three objects, select
Custom. You can then set layout values on the notebook tabs Chart, Title, and
Legend.

Width

This option specifies the width of the current object (chart, title, or legend) as a
percentage of the width of the entire graph control container.
For example, to set the width of a chart as half the width of the container,
specify the value 50 (50 percent of container width).
If you enter the value 0, the object is not displayed.

Height

This option specifies the height of the current object (chart, title, or legend) as
a percentage of the height of the entire graph control container.
For example, to set the height of a chart as half the height of the container,
specify the value 50 (50 percent of container height).
If you enter the value 0, the object is not displayed.

Left

This option specifies the distance from the left edge of the current object (chart,
title, or legend) to the left edge of the graph control container as a percentage
of the width of the container.

Top

This option specifies the distance from the top edge of the current object (chart,
title, or legend) to the top edge of the graph control container as a percentage
of the height of the container.

Anchor

This feature is not implemented.

Top, Bottom, Left,
Right (margin)

This option specifies margins around the current chart or legend in graphical
units. In a chart that has axis titles, margin settings determine the distance between the chart and the titles. In a legend, margin settings determine the distance
between legend items and the outside edges of the legend object.

Graph Border Box

When selected, this option encloses the entire graph component container in a
box border.

Infor Mongoose Application and Form Development | 879

Dialogs-NoTOC
Axis options
These options pertain to the X-axis.
Field/Option

Description

Max Points Displayed

This option specifies the maximum number of data points to render in the Xaxis. The graph ranges from the current object index to the number defined in
this field.

No Labels

When selected, this option prevents the display of X-axis labels. Labels are not
displayed, even if a property is selected in the Prop for X-Axis Labels field in
the Specify Graph Settings dialog box.

Title Position

This option specifies whether the title for the X-axis is to display above the graph
(Top) or below it (Bottom).
The default is Bottom.

Label Orientation

This option specifies what orientation to use for the X-axis data labels:
• Up orients the text vertically, rotating the text 90° left.
• Down orients the text vertically, rotating the text 90° right.
• Horizontal (default) orients the text horizontally (normal reading orientation).

These options pertain to the Y-axis.
Field/Option

Description

Maximum

This option specifies the maximum possible value for the range of values measured along the Y-axis.

Minimum

This option specifies the minimum possible value for the range of values measured along the Y-axis.

Title Position

This option specifies whether the title for the Y-axis is to display to the Left of
the graph or or to the Right.
The default is Left.

Title Orientation

This option specifies what orientation to use for the title of the Y-axis:
• Blank (default) orients the text horizontally.
• Up orients the text vertically, rotating the text 90° left.
• Down orients the text vertically, rotating the text 90° right.
• Vertical orients the text vertically (same as Up).

Auto Round/Truncate

When selected, this option does two things:
• It allows you to specify what units of display to use for the Y-axis scale
markers and the number by which the data values should be divided.
• It enables the Levels button.

Levels

This button is enabled only if the Auto Round/Truncate option is selected.
When enabled and clicked, it opens the Edit Divisors dialog box, which allows
you to set the options to display the scale markers on the Y-axis.

Infor Mongoose Application and Form Development | 880

Dialogs-NoTOC
Legend options
These options pertain to the display of the legend.
Field/Option

Description

Border Box

When this option is selected, the legend is enclosed by a border.

Horizontal

When this option is selected, legend items are arranged left-to-right, rather than
in a vertical stack.

Auto Text Color

When this option is selected, the system uses your specified text color in the
legend box.
When this option is cleared, the system uses the text (foreground) color of the
base form.

Position

This option sets the position of icons or boxes in legend items relative to the
text in the items.

Box Mode

This option sets the style in which legend items display with graphical elements:
• Icons pairs a graphical element with the text.
• Boxes pairs a colored box with the text.
• None displays only the text.

Others options
These options pertain to miscellaneous other options.
Field/Option

Description

Fonts group:
• Main Title
• Labels
• Other Titles
• Legends

These options specify what fonts are to be used in the display of various elements of the graph.

Reset to Default

To clear custom font specifications for the main title, labels, other titles, and
legends, click this button.

Runtime options
These options pertain to system behaviors associated with the graph.
Field/Option

Description

Lock Objects

When this option is selected, users are prevented from moving and resizing of
graph objects.
When this option is cleared, users can move and resize a graph, its title, and
its legend as separate objects.

Infor Mongoose Application and Form Development | 881

Dialogs-NoTOC

Field/Option

Description

Runtime Buttons

When this option is selected, Print, Copy, and Save buttons are enabled for
the graph, when the mouse pointer hovers over the graph component.

(Print/Copy/Save)

When this option is cleared, access to these buttons is disabled.
Runtime Properties

When this option is selected, the right-click menu command Chart Property
Pages, which displays configurable properties of a graph at run time is enabled.
When this option is cleared, that command is disabled.
Note: You can define a standard right-click menu, using the Right-Click Menu
property on the Component property page.

Mouse Hover Data Display

When this option is selected, the actual value of a graph element displays as
tooltip text when the mouse hovers over the element on the graph.
To improve the formatting of the tooltip text, specify the data type properties for
the graph element (for example, Underlying Type, Length, Decimal Point
Position, and so on); or specify an existing component class that specifies the
proper data type properties.

Browser Preview dialog box
Use the Browser Preview dialog box to preview the form in a web browser. The system connects to
the web server associated with the configuration server you are using. If you are running Windows
client locally, the system executes the preview using WSWebclient .
This dialog box is accessed in Design Mode from the menu option Form > Definition > Preview in
Browser.

Calendar Category dialog box
Use this dialog box to maintain categories for use in a calendar. A category is a type of activity to be
placed on the calendar. You can set up new categories and edit existing ones.
Open this dialog box by clicking New or Edit on the Attributes tab of the Calendar Settings dialog box
on page 882.

Calendar Settings dialog box
Use this dialog box to specify settings for a calendar component.

Infor Mongoose Application and Form Development | 882

Dialogs-NoTOC
To open this dialog box, add a Calendar component from the toolbox to a form, click the ellipses (...)
button next to the Binding property (under Data Source), select Calendar as the type in the Edit
Component Data Binding dialog, and click Edit.
This dialog box has these tabs:
•
•

Attributes
Activity Data

Attributes tab
On this tab, specify the attributes for the calendar.
Field

Description

Mode

Select the viewing mode for the calendar: day, week, or month. The
default is month.

Categories

Select the categories you want to use to distinguish activities on the
calendar using different colors.
To open the Calendar Category dialog box and create a new category
and color, click New.
To make changes to an existing category, click Edit.
To remove a category, click Delete.

Selected Item Variables

Use these fields to specify form variables that alert the user of which
activities have been selected.
• ID: From the drop-down list, select the variable that is to contain
the value for the activity ID.
• Start: From the drop-down list, select the variable that is to contain
the value for the start date of the activity.
• End: From the drop-down list, select the variable that is to contain
the value for the end date of the activity.
When an activity is selected, the variables are updated. For example,
a user could set the Start and End variables to send an alert when a
vacation day is selected on a calendar.

Activity Data tab
On this tab, specify the collection of data that is used in the calendar.
Field

Description

Collection

Select the form collection to which the calendar is bound.

Infor Mongoose Application and Form Development | 883

Dialogs-NoTOC

Field

Description

Properties

Specify these collection properties for the activity:
• ID
• Start
• End
• Subject
• Description
• Category

Component Class/Property Class Extension dialog
box
Use this dialog box to create, copy, edit, or delete component classes or property class extensions.
The list includes all component classes or all property class extensions that are stored in the forms
database as global objects.
•
•

To add a component class or property class extension, click New.
To copy, edit, or delete a component class or property class extension, select the one you want in
the list. Then click the appropriate button.

Component Templates dialog box
Use this dialog box to create a component template by saving a selected component and its
child-components as a template. You can use a component template to quickly add components in
Design Mode. Component templates are saved and listed within the Toolbox.
Field/Option

Description

Name

This is the name of the template. The default format is formNam
e/componentName.
The forward slash (/) is used to organize the templates shown in
the Toolbox.

Description

This is the component template description. This is used as a
Toolbox tree-node tooltip.

Infor Mongoose Application and Form Development | 884

Dialogs-NoTOC

Component properties sheet
Use the Component properties sheet to set properties of the current component on a form.
On the property sheet for a component, you can:
•
•
•
•
•

Select a component by name from a list of all components on the current form, using the drop-down
component list.
Sort properties by category.
Sort properties alphabetically.
View attributes on the property sheet for the component.
View event properties for a component.

For help with a specific property, right-click the property row and select Help from the context menu.

Copy Form Definition dialog box
Use this dialog box to create a new form by copying an existing form.
Field/Option

Description

Source Form

Select the name of the form being copied.

Target Form

Specify the name to assign to the form being created.

Copy Template Also

Select this check box to copy the source form's template and its definition. Clear this check box to copy only the form definition.

Copy All Device Types

Select this check box to copy all the device types. Clear this check box
to select the source device and target device type.

Source Device

Select the source device.

Target Master

Select the target device type. Select Default, Mobile, Laptop, or
Desktop.

Copy Form Definition Template dialog box
Use this dialog box to create a new form template by copying an existing template.
Field/Option

Description

Source Form Template

Specify the name of the form template being
copied.

Target Form Template

Specify the name of the new form template being
created.

Infor Mongoose Application and Form Development | 885

Dialogs-NoTOC

Copy to Clipboard button
Use this button to copy the script generated for a User-Defined Data Type (UDT) to the system clipboard.
This allows you to paste the script into a text or other file of your own.
This button is enabled only if you have one or more UDTs in your form definition.
See About the New Data Maintenance Wizard on page 107.

Design View Size dialog box
Use the Design View Size dialog box to change the design surface of a Report type form, without
having to change the actual size of the form.
This dialog box can be accessed only in Tabbed Form Layout mode. To access it, right-click on the
design surface outside of the actual form area.

Edit Action dialog box
Use this dialog box to specify the "action" part of a conditional action value expression for a component.

Infor Mongoose Application and Form Development | 886

Dialogs-NoTOC

Field/Option

Description

Action Type

Specify the type of conditional action to define:
• NONE - Nothing happens. This setting is the default when you
create a new conditional action.
• Set - Sets a value for the component if the condition evaluates to
TRUE.
• SetModified - Sets a value for the component and marks the record
as modified, if the condition evaluates to TRUE.
• Enabled - Enable the component if the condition evaluates to
TRUE, or disable the component if the condition evaluates to
FALSE.
• Required - Make the component a required field if the condition
evaluates to TRUE.
• Visible - Make the component visible if the condition evaluates to
TRUE, or hide the component if the condition evaluates to FALSE.
• Toggle - Turn the component setting on or off based on the condition setting.
• Modified - Mark the record as modified if the condition evaluates
to TRUE.
• GenEvent - Generate an event if the condition evaluates to TRUE.
• GenEventRet - Generate an event and return a value if the condition evaluates to TRUE.
• Focus - Sets focus on the component if the condition evaluates to
TRUE. If the condition evaluates to FALSE, focus on another
component, if specified in the expression.
• ReturnValue - Sets the return value based on the conditional action.
• Stop - If the condition evaluates to TRUE, stop execution of any
additional actions on the overall expression.
• ThemeClass - If the condition evaluates to TRUE, the given theme
class is applied.
• Validate - If the condition evaluates to FALSE, display the message
specified in the expression.
See Conditional Action Syntax on page 307.

Event Name

Enter a value or, to display the Edit Value Expression dialog box, click
Edit Value.

Specify form or component
(2 fields)

These two fields are only available when GenEvent or GenEventRet
is selected as the Action Type. Use these fields to specify the target
for the event to be generated:
• ThisForm - Specifies the current form as the target.
• Form name - Use the second drop-down list to select the form that
is to be the target.
• Component - Use the second drop-down list to select the component on the current form that is to be the target.

Infor Mongoose Application and Form Development | 887

Dialogs-NoTOC

Edit Aggregate Binding dialog box
Use the Edit Aggregate Binding dialog box to specify aggregate binding for Edit and Static components.
Field/Option

Description

Function

From the drop-down list, select what type of aggregate operation
to perform on the values returned for the component. For the types
of operations and how they work, see the Aggregate functions
table below.

Collection

From the drop-down list, select the IDO collection to use for the
aggregate operation.
Note: When you specify the collection, both the Property field
and the Group By list are populated with the available properties.

Property

From the drop-down list, select the property on which to perform
the aggregate operation.
For Minimum, Maximum, and Count operations, this list is automatically filtered to include only properties that use numeric, date/time,
and character data types. For Summation and Average operations,
this list is filtered to include only properties that use numeric values.

Group By

Optionally, use this option to specify a "group by" property. This
can be used for things like showing a subtotal.
For example, you might want to display a subtotal of Sales per
Customer. In this case, "SalesAmt" would be the property to select
for the Property field, and "CustomerID" would be the property to
select for the Group By option.
If, then, the current row’s CustomerID is “AcmeWidgets” this aggregate would total the sales amounts of all records in the current
collection where CustomerID = "AcmeWidgets".

Table 5: Aggregate functions
Operation

Description/Comments

(SUM)

This aggregate adds the values and returns the total. It can be
used only for properties with numeric values.

Minimum (MIN)

The value depends on the data type:
• Numeric value - Returns the least value.
• Date/Time value - Returns the earliest date/time.
• Character value - Returns the first character string ranked alphanumerically. For this calculation, the system uses the current system culture setting and ignores capitalization.

Infor Mongoose Application and Form Development | 888

Dialogs-NoTOC

Operation

Description/Comments

Maximum (MAX)

The value depends on the data type:
• Numeric value - Returns the greatest value.
• Date/Time value - Returns the most recent date/time.
• Character value - Returns the last character string ranked alphanumerically. For this calculation, the system uses the current system culture setting and ignores capitalization.

Average (AVG)

This operation returns the mean average of the values returned.
It can be used only for properties with numeric values.

Count (CNT)

This operation returns the number of records where the specified
property values are not null or not blank.

Edit Background Task Name and Parms dialog box
Use this dialog box to set up and maintain specifications for event handlers of the Run Background
Task response type.
Field/Option

Description

Task Name

The name of the background task to be run.
This task name must be defined to WinStudio on the Background
Task Definitions form (a Core form installed with Windows client).

Task Parameters

A comma-delimited list of parameters associated with the background
task to be run.

Edit Browser Binding Specification dialog box
Browser components on a form are bound to the Browser data binding type, which specifies behavior
that is unique to browser components. Use this dialog box to maintain information used to connect to
a URL or to display HTML code in the browser.
Field/Option

Description

Type

The type of information to display in the browser: URL link or inline HTML
code.

Infor Mongoose Application and Form Development | 889

Dialogs-NoTOC

Field/Option

Description

File Name

The file name and path of the URL, or the HTML code to display. If Type
is set to HTML, enter the HTML code for a working web page in this field,
for example:
<HTML>
<BODY> Hello world! </BODY>
</HTML>

Edit Browser File dialog box
Use this dialog box to create and edit various kinds of resources for use as web user control assets.
You can:
•
•

Create or edit a text-based resource, such as an HTML, CSS, JavaScript, or XML snippet.
Import or export an image resource, video clip, PDF file, or other user component.

Use the File field to specify the path and filename of the resource.
Use the Content Type field to specify the type of file to create or edit. Select any of these types:
•
•
•
•
•
•
•
•
•
•
•

text/HTML
text/css
text/javascript
text/xml
image/jpeg
image/png
image/gif
image/tiff
application/pdf
video/mp4
application/user-component

The other fields and options that are available in this dialog box depend on what Content Type you
select.
•

•
•
•

The default option is for an HTML page, with the basic HTML file structure prepopulated. Other
text-based options (for CSS, JavaScript, and XML) offer the same basic framework, which you can
then edit and overwrite as needed.
Image options display a preview of the image, with options to export or import image files.
Other multimedia types, such as audio files, video files, and so on, do not allow you to preview the
content.
The application/user-component option displays a page on which you can define the properties,
attributes, and required resources of a User Component you are creating.

Infor Mongoose Application and Form Development | 890

Dialogs-NoTOC

Edit Collection Action dialog box
Use this dialog box to specify the collection for event handlers using one of the collection processing
response types.
Field/Option

Description

Collection

The collection to be affected by this action.
The list displays collections on the current form.

Navigation Action

If the response type is Collection Navigate, specify the type of navigation to perform (Next, Previous, Last, First).

Edit Collection ID Binding dialog box
Use this dialog box to bind a group box component to a collection.
Field/Option

Description

Collection

The name of the collection bound to this group box.

Disable Raised 3D Display

When selected, this option specifies that two-dimensional outlining
is to be used for the group box.
To give the group box a raised, three-dimensional appearance when
it has focus, clear this check box.

Edit Collection Specification for Subcollection dialog
box
Use this dialog box to specify the properties of a subcollection.
See Edit Collection Specification for Subcollection properties and attributes.

Edit Color dialog box
Use this dialog box to define background colors and to assign those definitions to forms and components.
The background "color" can be either:
•

A single, solid color, with or without transparency

Infor Mongoose Application and Form Development | 891

Dialogs-NoTOC
•

A pair of colors used for a linear gradient, with each color having its own RGB and transparency
settings

You can use this dialog box to:
•
•
•

Create a color definition "from scratch".
Copy an existing color definition and modify it to create a new color definition.
Delete a color definition.

Any color definitions you create in this dialog box are maintained as global variables and are available
to all forms and components that can use them.

Edit Command or Edit URL dialog box
Use this dialog box to set up and maintain the specifications for an event handler with the Execute
EXE response type or the Go To URL response type.
Field/Option

Description

Command

This option specifies a command string that opens an application, a
document, or a URL(web page). Enter the command as you would
enter any Run command from the Windows Start menu.
To open a document associated with an application, enter the document path. You do not need to specify the application path.

Edit Component Data Binding dialog box
Use this dialog box to specify the type of binding for a component.
Field/Option

Description

Type

This field specifies the type of data binding.
For each type of component, only some data bindings are appropriate.
See Component data binding types on page 241.

Edit

Click to set up or maintain specifications for the type of binding selected.

Edit Condition dialog box
Use this dialog to specify a condition to include in a conditional action expression for a component.

Infor Mongoose Application and Form Development | 892

Dialogs-NoTOC

Field/Option

Description

Up

This option moves the selected condition clause up in the sequence.

New

This option creates a new row (clause) in the condition.

Delete

This option deletes the selected clause.

Down

This option moves the selected condition clause down in the sequence.

Left Value

This field displays the value set for the left side of the current clause in
the conditional expression.
To open the Edit Value Expression dialog box and specify a different
Value Type or Value for the current clause, click Edit Left Value.

Operator

This option specifies the operator to use to compare the Left Value with
the Right Value.

Right Value

This field displays the value set for the right side of the current clause
in the conditional expression.
To open the Edit Value Expression dialog box and specify a different
Value Type or Value for the current clause, click Edit Right Value.

And With Next Clause

When this option is selected the current clause and the next clause are
joined with a Boolean AND filter. Clauses joined with AND allow for only
those conditions that meet the criteria specified in all clauses.
When this option is cleared, the clause is defined as an OR clause.
Clauses joined by OR allow for conditions that meet any one of the criteria specified in the clauses.

Edit Contained Component Visibility and Order
(FlexLayout) dialog box
Use the Edit Contained Component Visibility and Order (FlexLayout) dialog box to show, hide,
and change the order of a form's FlexLayout components.
Use these fields or buttons to update its sequence and visibility:
Field or button

Description

Visible

Use this check box to display or hide FlexLayout components.

Name

This column lists the contained components of FlexLayout. The
components are grouped in region.

Caption

This column lists the captions or labels of the FlexLayout
components.

Up

Click this button to move the selected component higher or
farther to the left in the FlexLayout.

Infor Mongoose Application and Form Development | 893

Dialogs-NoTOC

Field or button

Description

Down

Click this button to move the selected component lower or farther to the right in the FlexLayout.

Show All

Click this button to select the Visible check box for all components.

Hide All

Click this button to clear the Visible check box for all components.

Note: Similar to grid components, the values you define become part of the runtime changes for the
form or component.

Edit Contained Components dialog box
Use this dialog box in Design Mode to add, configure, and delete components within a container
component. This dialog box is accessed by clicking the Edit button in the upper left corner of these
container components while in Design Mode:
•
•
•

Notebooks
Grids
Vertical Grids

For FlexLayout components, this dialog box is accessed by clicking the Edit button in the upper left
corner of the FlexLayout component, and then clicking the Contained Components button in the Edit
FlexLayout Attributes dialog box.
This dialog box consists of two basic sections:
•

The left side contains:
• The Items column, which displays the list of the contained components currently defined for
the container.
For example, if this dialog box is accessed from a grid, the list displays, in order, all the columns
currently defined for the grid.
For FlexLayout components, items are grouped by region.
•

The Sequence column, which displays the sequence number of the contained components.
For FlexLayout components, the sequence is ordered inside the contained component's
respective region.

•

These controls at the bottom:
Control

Description/Comments

Delete button

Deletes the selected component from the list

Move Up button

Moves the selected component up in the list

Move Down button

Moves the selected component down in the list

Infor Mongoose Application and Form Development | 894

Dialogs-NoTOC

Control

Description/Comments

Copy/Paste button

Copies the selected component to a selected container
This button opens the Select Container for Pasted Component
dialog box, which allows you to specify where and how the copied
component is to be pasted.

Container component display

(Read-only) Displays the name of the container to which the contained components are assigned
If there are multiple containers, you can select the one you want
from the list.

Add button
•

Adds a contained component to the list

The right side has two tabs:
• Properties displays a properties sheet for the selected contained component.
• Events displays an events sheet for the selected contained component.

These sheets are identical to their counterparts in the Design Mode property sheet displays in Windows
client. Once the contained component exists, you can configure it in either place and the results are
the same.

Edit Device Types
Use the Edit Device Types dialog box to create and edit a list of devices for which you can develop
forms. Each device can be identified by an ID, screen width, screen height, and user agent. ID zero
(0) exists by default, and represents the application's standard PC interface.
Note: Device screen size is measured in pixels.
The optional User Agent field allows you to identify specific browsers that are compatible with the
selected device. Separate browser names with the pipe symbol ( | ) (for example,
browser1|browser2|browser3).
To access this form, select Form > Definition > Edit Device Types.

Edit Divisors dialog box
Use the Edit Divisors dialog box to specify the units and divisors by which the Y-axis scale is to be
marked and displayed. For example, if the Y-axis data is to be displayed using millions of dollars, use
the fields in this dialog box to set that up. You would select the option that uses 1000000 as the Divisor
and sMillions as the String. When the form is opened and the graph is rendered, the data for the
Y-axis is then automatically divided by 1000000 and the label "Millions" appears next to the Y-axis.

Infor Mongoose Application and Form Development | 895

Dialogs-NoTOC
Note: Just because you select an option such as "Thousands," that does not force the system to use
that divisor. If the smallest data value being presented is not divisible by 1000 without the displayed
value being too small (for example, 20, which would display as ".02"), the system ignores this setting.
If the smallest data value being presented is large enough that an even bigger value can be used
instead (for example, 30000000, which is more easily and clearly divisible by one million), then the
system uses the larger divisor instead, in this case 1000000.
This dialog box provides three standard divisors and units, but you can define your own. You can, for
example, define the Y-axis data to render as minutes for raw data provided as the number of seconds.
In this case, you would use 60 as the Divisor and sMinutes as the String.
Note: If the string you want is not already defined on the system, and you do not define it using the
String Properties dialog box, whatever you use in the String field is treated as a literal value. So, for
example, if you try to use sMinutes as the string but do not define it as a recognized string on the
system, the graph actually displays "sMinutes".

Edit Drag From/Drop On Events dialog box
Use this dialog box to make "Drag From" and "Drop On" event assignments.
Note: The exact name of this dialog box depends on the context from which it is called: If it is being
opened to assign "Drag From" events, it is labeled the Edit Drag From Events dialog box. If it is being
opened to assign "Drop On" events, it is labeled the Edit Drop On Events dialog box. In both cases,
however, the functionality is the same.

Edit Entry dialog box
Use this dialog box to:
•
•

Define a new parameter.
Modify an existing parameter.

Infor Mongoose Application and Form Development | 896

Dialogs-NoTOC

This field:

Does this:

Type

Specifies the type of parameter to create. Options include:
• Literal: Creates a parameter that passes a literal string value
to the component class.
With this type, specify the value to be passed in the Value
field.
•

Property Name: Creates a parameter that passes the name
of an IDO property.
With this type, you must specify both a property from the Value
drop-down list and the IDO Collection that the property belongs
to.

•

Variable Name: Creates a parameter that passes the name
of a variable.
With this type, you must specify the name of the variable from
the Value drop-down list.

•

Component Name: Creates a parameter that passes the
name of a component on the form.
With this type, you must specify the name of the component
from the Value drop-down list.

•

Property Value: Creates a parameter that passes an expression that contains the name of the property and the collection
to which it belongs. At run time, then, the value of the property
is passed along. The syntax for this is P(collection.propertyName) .
With this type, you must specify both a property from the Value
drop-down list and the IDO Collection that the property belongs
to.

•

Variable Value: Creates a parameter that passes an expression that contains the name of the variable whose value is to
be passed along. The syntax for this is V(variableName) .
With this type, you must specify the variable whose value is
to be passed along, from the Value drop-down list.

•

Component Value: Creates a parameter that passes an expression that contains the name of the component whose
value is be passed along. The syntax for this is C(componentName) .
With this type, you must specify the component whose value
is be passed along, from the Value drop-down list.

Value

Specifies the value for the parameter.
In most cases, this field is populated, as a drop-down list, with
values appropriate to the Type specified. The exception is the
Literal type, which requires that you enter the parameter's value
directly in this field.

Infor Mongoose Application and Form Development | 897

Dialogs-NoTOC

This field:

Does this:

Collection

Specifies the IDO collection to which a selected property belongs.
This drop-down list is enabled, populated, and required only if you
are defining a Property Name or Property Value parameter.

Edit Event To Generate dialog box
Use this dialog box to set up and maintain the specifications for an event handler with the Generate
Event response type on page 171.
Field/Option

Description

Event

The name of the standard event or custom event to be generated by
the event handler.

Post event asynchronously

To have the system generate the event handler after the current
processing is completed, select this field.

Edit Expression Binding dialog box
Use the Edit Expression Binding dialog box to specify expression binding for Edit and Static
components.
This expression editor allows you to:
•
•
•

Break down the expression into functions and operands
Recursively edit embedded functions
Validate the expression

The expression is stored in the component data source in the form of expression( expression
).
Field/Option

Description

Expression

Displays the expression that is constructed based on the function or operator that
you selected

Infor Mongoose Application and Form Development | 898

Dialogs-NoTOC

Field/Option

Description

Function

Select what type of expression function or operator to perform on the values returned for the component:
• Math operators: NEGATE, +, −, *, /, %
• Math functions: ABS, CEILING, FLOOR, MAX, MIN, POWER, ROUND, SIGN,
SQRT, TRUNC
• Comparison operators: == or =, >=, <=, >, <, != or <>
• Logical operators: AND or &&, NOT or !, OR or ||
• Logical functions: CONTAINS, IF, ISEMPTY
• Text functions: CONCAT, INSTR, LEFT, LEN, LOWER, LTRIM, REPLACE, RIGHT,
RTRIM, SUBSTRING, TRIM, UPPER
• Accumulator functions: AVGACCUM, CNTACCUM, MAXACCUM, MINACCUM,
SUMACCUM
• Aggregate functions: AVG, AVGEXP, CNT, CNTEXP, MAXAGG, MAXEXP, MINAGG,
MINEXP, SUM, SUMEXP
• Date functions: CURDATE, CURDATETIME, DATE, DATEADD, DATEDIFF,
DATEPART, ISOWEEKNUM
• Mongoose functions: C, FORMAT, P, STRINGS, USERNAME, V
• DataView functions: AVG, AVGEXP, CNT, CNTEXP, MAXAGG, MAXEXP, MINAGG,
MINEXP, SUM, SUMEXP, DATAVIEWPROP, DATAVIEWEXP
See the Expression functions and operators topic.

Validate

Click this button to validate the expression.

Edit

Click this button to recursively open this expression editor to build or edit the
operand value.

Edit Filter dialog box
Use this dialog box to maintain filter criteria. Use these options.
All Available Properties section
These options pertain to the IDO referenced in the Identification section.
Field/Option

Description

Drop-down list of properties

This is a list of all the properties defined for the IDO collection with
which the filter is associated.

Add To Additional

Clicking this button adds the selected property to the list of Additional
Criteria Properties.
These are the properties that are available for selection on the Additional Criteria tab of the query form.

Infor Mongoose Application and Form Development | 899

Dialogs-NoTOC

Field/Option

Description

Reload

Use this button to refresh the list of IDO properties.
This can be useful in cases where the list of IDO properties might
have changed since you began.

Primary Criteria Properties section
This section lists the properties that are displayed on the Primary Criteria tab of the query form. This
list is read-only. You cannot add to it, remove from it, or otherwise modify it.
Identification section
The data displayed in this section is read-only and includes this information:
•
•
•
•

The Name of the filter
The IDO (Object) with which the filter is associated
The editing Scope level at which the filter was created
If applicable, the name of the user or group (User/Group) for which the filter is accessible

Additional Criteria Properties section
This displays a list of the properties to be accessible on the Additional Criteria tab of the query form
for this filter.
Field/Option

Description

Attributes

Use this button to edit the attributes of a selected property, including
its label on the query form, its list source specifications, and so on.

Remove

Use this button to remove a selected property from the list.

Edit Filter Binding dialog box
Components on a query form are bound to the Filter Attribute data binding type, which specifies behavior
that is unique to components on a query form. For example, a component can function as an operator
for primary criteria, a value for additional criteria, a label, and so on. Use this dialog box to maintain
the type of Filter Attribute associated with a component on a query form.
Field/Option

Description

Type

The type of Filter Attribute associated with this component on a query
form.

Primary Property

The name of the IDO property included as primary criteria on this
query form.

Infor Mongoose Application and Form Development | 900

Dialogs-NoTOC

Edit FlexLayout Attributes dialog box
Use the Edit FlexLayout Attributes dialog box to set the attributes and properties for FlexLayout
regions.
This dialog box is accessed by clicking the Edit FlexLayout button in the upper left corner of any
FlexLayout component or component contained within a FlexLayout component.

Edit Form/Global Script dialog box
Also known as the Windows client script editor, use the Edit Form/Global Script dialog box to create,
edit, and debug form scripts and global scripts.
The type of script you develop depends on how you access this dialog box:
•

With a form open and in Design Mode, you can access this dialog box using:
• The Edit menu, Script Editor option
• The Edit Form Script button in the top left corner of the Form property sheet
Either of these actions signals that you are developing a form script by opening this editor as the
Edit Form Script dialog box.

•

In Design Mode, with or without a form open, you can access this dialog box using the Edit menu,
Script option.
This action signals that you are developing a global script by opening this editor as the Edit Global
Script dialog box.

Regardless of which type of script you are developing, the options and procedures are virtually the
same.

Edit Form Pages dialog box
Use this dialog box to create, edit, or delete form page on page 193 components for the current form.

Edit Form Relationship Specification dialog box
Use this dialog box to link a child form to a new parent form you are creating.
Field/Option

Description

Child Form Name

The name of the child form being linked to the parent form.

Infor Mongoose Application and Form Development | 901

Dialogs-NoTOC

Field/Option

Description

Child Form Parms

Specifications for opening the child form.
Enter WinStudio keywords and their parameters.Omit the LINKBY
keyword, but include any other appropriate keywords such as FILTERPERM or SETVARVALUESONLINKUPDATE.

Property Pairs By Which To
Link

The properties that link the parent and child form.
To add property pairs, click New. To edit or delete a property pair,
select the pair in the list and click Edit or Delete.

Edit Form Template Properties dialog box
Use this dialog box to edit the IDO properties included as components in a form template.
The list includes all the properties for the IDO whose collection is bound to this form template.

Edit Form Template Property Attributes dialog box
Use this dialog box to specify how an IDO property is to be represented by a component on a form
template.
Field/Option

Description

Caption

This option specifies the caption of the component for the property.
You can enter literal text, select a string, or click the button to create
or edit a string.

Include in Grid

When this option is selected the resulting component is also displayed
in in the Grid View. To omit the component from the Grid View, clear
this check box.
This option applies to multiview forms only.

Containment

This option specifies what type of container holds the component, if
any. These are the possible options:
• Default signifies that the component is not in any container.
• Form Page specifies that component is to be displayed on one
or more form pages.
• Notebook specifies that the component is to be contained within
a notebook tab.

Infor Mongoose Application and Form Development | 902

Dialogs-NoTOC

Field/Option

Description

Container Sequence

For components located within containers, this option specifies the
sequence number for the component within the container.
Windows client manages this sequence for most containers. For
form page components, you must specify a zero-based sequence
for each form page component.

Component Class

This field specifies the name of a component class associated with
this component.
To edit the component class, click the button.

Component Class Parms

This field specifies any parameters that are to be passed to validators
defined in the component class.
Parameters are usually a string of Windows client keywords and their
parameters.

Edit Graph Binding dialog box
Use this dialog box to set up and maintain button or edit components that users use to adjust a graph
component.
Note: This topic applies to the graphing tool, which is supported for backward compatibility. We
recommend that, if you want to add charts or gauges to a form, use the newer FusionCharts tool instead.
See About charts and gauges on page 183.
Field/Option

Description

Previous Button

This option applies to buttons that are used to scroll the graph display to the preceding section. These buttons are automatically labeled Previous.

Next Button

This option applies to buttons that are used to scroll the graph display to the following section. These buttons are automatically labeled
Next.

Redraw Button

This option applies to buttons that are used to redraw the graph,
employing any new user settings. These buttons are automatically
labeled Redraw.

Properties Button

This option applies to buttons that are used to adjust the color, style,
or other display properties of the graph. These buttons are automatically labeled Properties.

X Limit

This option is used to specify the maximum number of points that
can be included on the X-axis.

Graph component

This option specifies the name of the associated graph component.

Infor Mongoose Application and Form Development | 903

Dialogs-NoTOC

Edit Inline List Entries dialog box
Use this dialog box for Inline list sources, to create the hard-coded table of values that becomes the
list source for this component.
• To add or remove columns, use the New Col or Delete Col buttons.
• To add or remove rows, use the New Row or Delete Row buttons.
• To enter a value, click in the cell and enter the text you want.
Note: Windows client interprets list values as string names. If a list value corresponds to a string name,
Windows client uses the string value as the list value. If the list value does not correspond to a string
name, Windows client treats the list value as a literal.

Edit Link Property Pair dialog box
Use this dialog box to specify the properties that link a parent and child form.
Field/Option

Description

Parent Form Property Name

This field specifies the name of the property on the parent form
that is to be linked to the child form.

Child Form Property Name

This field specifies the name of the property on the child form that
is to be linked to the specified property on the parent form.

Edit LinkBy Specification Dialog Box
For a Grid component bound to a subcollection, use this dialog box to set the parent-and-child
relationships between the primary collection and the subcollection.
For a Tree component, use this dialog box to specify relationships between levels of the tree. Nodes
at one level link to nodes at a child sublevel in a collection-subcollection relationship.
Select Child and Parent Property section
Field/Option

Description

Parent

This field specifies a property in the primary collection that is to be linked
to a subcollection property.

Child

This field specifies a property in the subcollection that is to be linked to
the specified primary collection property.

Add button

Use this button to add the parent-child property pair to the data binding
specification for this component.

Infor Mongoose Application and Form Development | 904

Dialogs-NoTOC
Property Pairs Linking Parent to Child Collection section
This section lists the parent-child property pairs that link the grid component for the subcollection to
the primary collection.
Use the Remove button to delete selected parent-child property pairs from the data binding specification
for this component.

Edit List of Component Class Parameters dialog box
Use the Edit list of Component Class Parameters dialog box to create and maintain lists of parameters
required by component classes assigned to a component. You can:
•
•
•
•

Add new parameters, using the New button.
Edit existing parameters, using the Edit button.
Rearrange the list of parameters, by selecting parameters and using the Up and Down buttons.
Delete parameters from the list.

To access this dialog box, use the ellipsis (...) button for the Component Class Parameters property
of a component (in Design Mode).

Edit List Source Specification dialog box
Use this dialog box to set up and maintain the specifications for a list source associated with a
component. The specifications vary with the type of list source being created.
Field or Group Box

Description

Type

This option specifies what type of list source is to be used:
• IDO Collection List Source on page 244
• IDO Method List Source on page 244
• Inline List Source on page 243
• Script List Source on page 245

Method and Collection Only
section

Applies to:
• IDO Collection type on page 244
• IDO Method type on page 244

Inline Only section

Applies to: Inline type on page 243

Method Only section

Applies to: IDO Method type on page 244

Script Only section

Applies to: Script type on page 245

Collection Only section

Applies to: IDO Collection type on page 244

Infor Mongoose Application and Form Development | 905

Dialogs-NoTOC

Field or Group Box

Description

Columns to Display

Applies to:
• All list source types
• IDO Collection type on page 244
• IDO Method type on page 244
• Inline type on page 243
• Script type on page 245

Edit Load Collection Event Handler Parameters dialog
box
Use this dialog box to edit the parameters that determine what IDO collection is to be queried and how
the results are to be returned and output.
This dialog box is accessed only by means of the Event Handler Parms dialog box, Type Specific
Parameters button for an event of type Load Collection.
Use these fields and options to determine what IDO collection is to be loaded and how:
Field/Option

Description

IDO

Use this option to specify what IDO collection is to be queried. You
can:
• Use the drop-down list to select the IDO.
OR
•

Set Prop/Var/Comp

Click the button to open the Select IDO dialog box and make
your selection.

Use this option to specify what properties, variables, and/or components are to be the targets for the return values for the IDO
collection. You can:
• Click the button to open the Edit Set Values dialog box, which
you can use to make your specifications.
OR
•

Mark Output Targets Validated

Type the specifications directly into the field.

When this option is selected, output targets do not validate before
navigating or saving the collection.
When cleared (the default behavior), output targets are marked
as requiring validation before navigatng or saving the collection.

Infor Mongoose Application and Form Development | 906

Dialogs-NoTOC

Field/Option

Description

Set Output Null on Null

When this option is selected, the output targets are cleared if no
records are returned.
When cleared (the default behavior), no updates are done if no
records are returned.

Filter, etc.

Use this option to specify a filter for the query. You can:
• Click the button to open the Specify Collection Options dialog box, which you can use to create the filter.
OR
•

Type the specifications directly into the field.

Edit Message Text
Use this dialog box to create complex messages for a prompt.
Messages can consist of any of these options:
•
•
•

String IDs are translatable strings stored in the system, created using the Strings dialog box.
When this option is selected, select the string from the drop-down list.
Literals are strings that display exactly as you create them. They are not translated.
Formatted Text are the most complex messages you can create. Any text you enter is treated as
a literal string value, but you can use substitution values to present property values such as form
captions or entered values. For substitution values, assign each place in the text that you want to
be substituted at runtime with a percent sign (%), followed by a number (n). At runtime each defined
value is substituted with the current value in place of the %n. Use the grid at the bottom of the form
to define substitution values.

Edit Message to Web Container dialog box
Use this dialog box to format a message to be sent to another web application. The web application
can be either Infor Ming.le or another Windows client frame.

Infor Mongoose Application and Form Development | 907

Dialogs-NoTOC

Field/Option

Description

Message Type

Specify a message type:
• sendMessageToContainer sends the message to the child Windows
client frame. The JSON message (which does not actually have to
be JSON in this case) is passed to the child frame without parsing
and is handled by the StdWebContainerMessageReceived event
handler in the child frame's form.
• openForm tells the child frame to open a new form after closing any
form currently open in the frame. The JSON message sends a "parameter" that contains the same parameters that are used when you
launch a form through a URL. After the form is opened, you can use
a "sendMessageToContainer" request to manipulate the form.
• refreshForm allows a currently opened form to be refreshed with a
new filter.
• registerMessageHandler allows a user to register and monitor for
messages that are sent through the Workspaces proxy. If using this
message type, the JSON Message field takes a comma delimited
list of messages you want to monitor for. When you monitor for a
message, typically you would then use the StdWebContainerMessageReceived event handler and check the StdWebPageMessageContext form variable for that message type and StdWebPageMessageData for the data.
• (other) If you specify a message type that is not known to the Windows client application, it is by default sent to the web page container
which is expected to be Infor Ming.le. The JSON message format is
defined by Infor Ming.le or context applications hosted within Infor
Ming.le. Each property must be surrounded by quotation marks.

JSON Message

Use this field to create the actual message to be sent (not necessarily
JSON).
The message format is defined by the message type, as described above.

Examples
Launch a form in the child frame: This example uses the openForm message type to open the Item
Stockroom Locations form in the child frame, with the filter based on the current form's Item property,
and refreshes the form with that filter. The JSON message specifies these parameters:

{
"parameters": "ItemStockroomLocations(FILTER(Item=FP(Item))
SETVARVALUES(InitialCommand=Refresh))"
}

Infor Mongoose Application and Form Development | 908

Dialogs-NoTOC

Refresh a filter: This example uses the refreshForm message type to refresh the filter in the child
frame from the previous example.
The JSON message specifies these parameters:

{
"filter": "Item=FP(Item)",
"form":"ItemStockroomLocations"
}

Send a message to Infor OS: This example sends a currency value, along with the base and target
currency, from Windows client to Infor OS for currency conversion.
In this example, the Message Type is set to currencyExchange, which is not a message type known
to Windows client. It is a message type supported by Infor OS. The JSON message format is also
defined by Infor OS:

{
"baseCurrency": "USD",
"baseValue": "V(currencyValue)",
"targetCurrency": "JPY"
}

Edit Method Call dialog box
For event handlers using the Method Call response type, use this dialog box to specify the name of
the IDO that contains the method, the method name, and any parameters required by the method.
Field/Option

Description

IDO

This field specifies the name of the IDO that contains the method
you want to call.

Method

This field specifies the name of the method called by this event
handler.

Parameters

This field specifies the parameters required by the IDO method
being called.

Infor Mongoose Application and Form Development | 909

Dialogs-NoTOC

Edit Method Parameter dialog box
Use this dialog box to specify the parameters required by an IDO method call. You can specify a literal,
property, variable, or component value as a parameter. Method call validators can also output values
to properties, variables, or components.
Field/Option

Description

Type

This field specifies the type of parameter required by the IDO method.

Output

When this option is selected, it signifies that a method call validator outputs values to a property, variable, or component.
If the method call validator does not output values, clear this option.

Value Type

This field specifies the type of value required by the parameter of the
IDO method. You can specify a literal, property, variable, or component
value as a parameter.

Value

Depending on the Value Type, enter a literal value or select a property,
variable, or component that is to supply the value.

Edit Target

If the Value Type is Variable or Component, click this button to edit
the variable or component referenced.

Edit Method Parameters dialog box
Use this dialog box to list in order the parameters required by an IDO method call.

Edit Middleware Load/Update Overrides dialog box
Use this dialog box to select which type of operation is to be handled by custom processing in the IDO
instead of by a standard operation on the collection. The list includes WinStudio keywords that
correspond to types of processing.
In the list, select a keyword and click Edit.
Option/Keyword

Description

UPD

This option/keyword is used to update existing records.

INS

This option/keyword is used to add (insert) new records.

DEL

This option/keyword is used to delete specified records.

Infor Mongoose Application and Form Development | 910

Dialogs-NoTOC

Option/Keyword

Description

PQ

This option/keyword is used to perform post-query processing in the IDO
before the data is returned.
This option can be used in combination with any of the other three.

Edit Optional Filters dialog box
Use this dialog box to edit a collection's Optional Filters property.
This dialog box is accessed by clicking the ellipsis (…) right next to the Optional Filters property in
the design mode.

Edit Parameter dialog box
Use this dialog box to specify parameters for a named method action that is part of the specification
for the Windows client UPD (Update), INS (Insert, DEL (Delete), or POSTQUERY keywords.
These keywords set up custom processing in the IDO that overrides the corresponding standard
operation on the collection.
Field/Option

Description

Type

This field specifies the type of parameter required by the IDO.
You can specify a literal value or a property value.

Output

When this option is selected, it signifies that the method outputs
values.
If the method does not output values, clear this check box.

Value

This field specifies the value to be used for the parameter. What
you do here depends on the Type that has been specified:
• If you specified a literal value for the Type, then enter that literal value in this field.
• If you specified a property value for the Type, then select from
the drop-down list the property value to be used.

Infor Mongoose Application and Form Development | 911

Dialogs-NoTOC

Edit Parameters for "Generate Application Event"
Event Handler dialog box
Use this dialog box to specify settings for generation of an application event with the Generate Application
Event response type.
For information about application events, see the help for the Events form and related forms.
Field/Option

Description

Event

This required field specifies the name of an application event defined in the Events form.
Only custom, non-framework events can be specified.

Synchronous

With this option is selected, form processing is suspended until
the Application Event System executes its event handlers.
Output values returned by handlers for the application event can
be processed into form components, variables, or properties, as
specified in parameter Value settings. Any updated components
and properties are marked as modified.
If you clear this check box, no processing of output parameters
occurs in the form. The form continues to execute after the event
is generated, allowing the application event system to operate in
parallel with Windows client.

Name

This field specifies the name of a parameter defined for the event
in the Application Event System.
To return the result of a synchronous event to the form component,
variable, or property specified in the Value field, enter the keyword
RESULT.

Value

This field specifies the value to be used for the parameter.
Windows client substitution keywords and literals are supported
as parameter values.
To add or edit a literal value or a component, variable, or property
value, click Edit Parameter.
If Synchronous is selected and a parameter with the same name
is defined as an output parameter in an application event handler,
output values can update form components, variables, or properties. To enable updates of this type, use the substitution keywords
C(ComponentName), V(VariableName), or P(PropertyName) as
values. Event handler output from such parameters is placed in
the named components, variables, or properties.

New Parameter

Click to create a new row in the Parameters grid.
To specify a new parameter, enter a Name, and then enter a
Value or click Edit Parameter to specify a Value.

Delete Parameter

Click to delete the current row in the Parameters grid.

Infor Mongoose Application and Form Development | 912

Dialogs-NoTOC

Field/Option

Description

Edit Parameter

Click to edit the Value in the current row in the Parameters grid.

Edit Parameters for Generate Application Event
Validator
Use this dialog box to specify settings for a validator with the Generate Application Event response
type. This type of validator lets you use the Application Event System to construct validation logic and
use that for component validation.
Field/Option

Description

Event

This required field specifies the name of an application event defined in the Events form.
Only custom, non-framework events can be specified.

Synchronous

With this option is selected, form processing is suspended until
the Application Event System executes its event handlers.
Output values returned by handlers for the application event can
be processed into form components, variables, or properties, as
specified in parameter Value settings. Any updated components
and properties are marked as modified.
If you clear this check box, no processing of output parameters
occurs in the form. The form continues to execute after the event
is generated, allowing the application event system to operate in
parallel with Windows client.

Name

This field specifies the name of a parameter defined for the event
in the Application Event System.
To return the result of a synchronous event to the form component,
variable, or property specified in the Value field, enter the keyword
RESULT.

Infor Mongoose Application and Form Development | 913

Dialogs-NoTOC

Field/Option

Description

Value

This field specifies the value to be used for the parameter.
Windows client substitution keywords and literals are supported
as parameter values.
To add or edit a literal value or a component, variable, or property
value, click Edit Parameter.
If Synchronous is selected and a parameter with the same name
is defined as an output parameter in an application event handler,
output values can update form components, variables, or properties. To enable updates of this type, use the substitution keywords
C(ComponentName), V(VariableName), or P(PropertyName) as
values. Event handler output from such parameters are placed in
the named components, variables, or properties.

New (Parameter)

Click to create a new row in the Parameters grid.
To specify a new parameter, enter a Name, and then enter a
Value or click Edit Parameter to specify a Value.

Delete (Parameter)

Click to delete the current row in the Parameters grid.

Edit (Parameter)

Click to edit the Value in the current row in the Parameters grid.

Edit Parameters for Invoke User Control Event Handler
Use this dialog box to edit the parameters for an event handler of the response type Invoke User
Control.
Field/Option

Description

User Control Component

This field specifies the name of a component that hosts a user
control.

Parameter Data

This field specifies the parameters that must be passed to the
invoked user control.
The parameter syntax depends on the requirements of the user
control itself. Windows client substitution keywords are supported.

Edit Permanent Filter dialog box
Use this dialog box to edit a collection's Permanent Filter Expression. This dialog box assists in editing
filter expressions by displaying several lines of a filter while it is being edited. The dialog box adds
optional support to help you in building the filter.

Infor Mongoose Application and Form Development | 914

Dialogs-NoTOC
This dialog box is accessed by clicking the ellipsis (•••) next to the Permanent Filter Expression
property.

Edit Prompt dialog box
Use this dialog box to set the parameters for the Prompt response type. These parameters include
these keywords:
Keyword

Description

PROMPT

Use this keyword to set the text to display in the prompt dialog.
Examples:
PROMPT(Would you like to continue?)
PROMPT(V(QUESTION))
PROMPT(STRINGS(sQuestion))
PROMPT(FORMAT(sDoYouWantToDelete%1,P(ID)))

CAPTION

This optional keyword overrides the default prompt dialog caption.
Examples:
CAPTION(Warning!)
CAPTION(STRINGS(sAnswerMyQuestion))
CAPTION(FORMAT(s%1WillBeDeleted,P(ID)))

RESPONSE

This keyword sets the prompt response options. These must use
this syntax:
RESPONSE(OPTION[eventToGenerate])
where OPTION can be any of these responses:
OK | CANCEL | YES | NO | ABORT | RETRY | IGNORE
The optional parameter eventToGenerate is the event that is
generated if this response is selected by the user.
Example:
RESPONSE(OK(PromptOK),CANCEL())
This example displays a prompt dialog with OK and Cancel buttons, and the OK button generates the PromptOK event in the
calling form.

Infor Mongoose Application and Form Development | 915

Dialogs-NoTOC

Keyword

Description

ICON

This optional keyword sets the icon to display in the prompt dialog.
The parameter can be one of these icons:
DEFAULT | ERROR | WARNING | QUESTION
The parameter corresponds to one of the standard framework
icons.
Example:
ICON(QUESTION)
If omitted, the icon is set to DEFAULT (blue circle with a white "i"
in the center).

DEFAULTRESPONSE

This optional keyword sets the default response button.
The parameter can be one of these responses:
OK | CANCEL | YES | NO | ABORT | RETRY | IGNORE
Example:
DEFAULTRESPONSE(OK)
The default response parameter must be one of the buttons
specified in the RESPONSE keyword.

CONDITION

This optional keyword sets the conditional action expression used
to determine if the prompt event handler is run.
Parameter:

STOPONCONDITIONTRUE

Expression: Use a conditionalThis optional keyword indicates that
the event chain should stop if the Prompt When condition is True.
This is useful when the prompt is tied to standard framework
events.

Edit Property Attributes for Filter dialog box
Use this dialog box to set up and maintain specifications for IDO properties being included in a filter
as either primary criteria or additional criteria. Properties included as primary criteria become components
on the Primary Criteria tab of the query form. Properties included as Additional Criteria become items
in the drop-down list on the Additional Criteria tab of the query form.

Infor Mongoose Application and Form Development | 916

Dialogs-NoTOC

Field/Option

Description

Label

When used for primary criteria, this option becomes the caption used to
label the Edit component for this property on the query form.
When used for additional criteria, this option becomes the name used to
identify the property in the drop-down list on the query form.
You can either enter the literal text of the label or select a translatable
string. To edit the string, click the button.

List Source

This option specifies the list source for the property.

Value Is List Index

If the property has a list source, selecting this option sets the property's
value to the zero-based index of the selected item. If the first item is selected, the property's value is set to 0; if the second item is selected, the
property's value is set to 1; and so on.
To disassociate the property's value from the index of the current list, clear
this check box.

Default Form

If the property uses a component class as the list source, select the name
of the component class from the drop-down list.
The list index setting of the component class supplies the list source.
To edit the component class, click the button.

Edit Property Binding dialog box
Use this dialog box to specify a collection property to be used as the data binding for a component.
Grid column components (and other components) that display a single value can be bound to an
individual property of the collection that is bound to the form (or to the grid component).
Field/Option

Description

Collection

This field specifies the name of the IDO collection to be bound to the
current form or current grid component.

Property

This field specifies the name of the property in the IDO collection to
which the component is to be bound.

Load Just in Time

When this option is selected, binary large object (BLOB) data is displayed only when the component has focus.
To have the system display the BLOB data when the form is opened,
clear this check box.
Note: This option applies only to properties that contain BLOB data.

Infor Mongoose Application and Form Development | 917

Dialogs-NoTOC

Edit/Select String dialog box
Use this dialog box to select, edit, or create a string object for a text element, such as a title, in a graphic
component.
Field/Option

Description

String

This field specifies the name of the string to be displayed.
You can select an existing string name from the drop-down list, or enter
a new name for a new string. To create a new string object with the new
string name, click Edit.

Edit

Click to edit an existing string's value or to create a new string object.
Caution: Be careful when editing existing string objects. Keep in mind
that, if the object is used in other places, all uses of the string will be
affected by the change.

Edit Set of Conditional Actions dialog box
Use this dialog box to express a set of conditional actions for a component.
If you reference variables, other components, or properties on collections on the form, Windows client
creates dependencies so that when those values change, the condition is automatically executed. For
example, you can specify that a certain component must be required when users specify a certain
value for another component.
Any existing conditional actions for the currently selected component are displayed in the left pane.
Each conditional action is displayed on a separate line.
Field/Option

Description

Up / Down

These buttons move the selected expression up or down in the
list.

New

This button opens the Edit Condition dialog box, which allows you
to create a new expression.

Delete

This button deletes the selected expression.

Edit Action

This button opens the Edit Action dialog box, where you can
specify a different action or value for the selected row.

Edit Condition

This button opens the Edit Condition dialog box, where you can
specify a different condition for the selected row.

Edit Value

This button opens the Edit Value Expression dialog box, where
you can specify a different value type and value for the selected
row.

Infor Mongoose Application and Form Development | 918

Dialogs-NoTOC

Edit Set <Variable | Component | Property> Value Pair
dialog box
Use this dialog box to set up and maintain the variables, properties, or components being used in event
handlers with the Set Values response type.
Field/Option

Description

Target

Select the variable, component, or property that is the target.
To edit a variable or component, click the Edit Target button.

Value

Specify the value associated with the target.
Enter values as a comma-delimited list (ValueA,ValueB,ValueC,
etc.).

Edit Set <Variable | Component | Property> Values
dialog box
Use this dialog box to set up and maintain variables, components, or properties used in event handlers
or validators of the following types:
•
•

Event handlers with the Set Values response type
Validators of the In Collection, In Inline List, In List Cache, Method Call, and Set From List Only
types

To reorder the list, select an item and click Up or Down. This change reorders the parameters for the
event handler or the validator.
•
•

To add an item, click New.
To edit or delete an item, select one and click Edit or Delete.

Edit Set <Variable | Component | Property> dialog box
Use this dialog box to set up and maintain variables, components, or properties, depending on the
context or what parent dialog box calls up this dialog box.
Validators of several event response types can output values based on variables, components or
properties. These response types include In Collection, In Inline List, In List Cache, Load Collection,
Method Call, Run Form, and Set Values.
Depending on the context in which this dialog box displays, the Edit Target and Edit Value buttons
might or might not be displayed.

Infor Mongoose Application and Form Development | 919

Dialogs-NoTOC
This dialog box prompts users for the target and source for a given clause from a set values specification.
The fields in the dialog box appear depending on the context in which it is displayed.
For InCollection validators and LoadCollection event handler parameters, the caption for this dialog
box is Edit Set <Variable | Component | Property> From List.
Field

Description

Target

This field displays either a list of components on the current form (if
this is a Set Component clause), global and form variables for the current form (if this is a Set Variables clause); or a list of properties on the
selected collection on the current form (if this is a Set Properties clause).
Select the variable, component, or property that is the target that receives the output value.
To open the properties dialog for the selected target component (if this
is a Set Component clause) or variable (if this is a Set Variable clause),
click Edit Target. This button is not shown if this is a Set Properties
clause.

Value

This field displays a list of properties for the IDO used for the collection.
Select a property from the list.

Collection for Target

This field displays a list of collections on the current form. Select a
property from the list.
Note: This field displays only if this is a Set Properties clause.

Set Values parameters
For Set Values event handler parameters, the caption for this dialog box is Edit Set <Variable |
Component | Property> Pair.
Field

Description

Target

This field displays either a list of components on the current form (if
this is a Set Component clause), global and form variables for the current form (if this is a Set Variables clause); or a list of properties on the
selected collection on the current form (if this is a Set Properties clause).
Select the variable, component, or property that is the target that receives the output value.
To open the properties dialog for the selected target component (if this
is a Set Component clause) or variable (if this is a Set Variable clause),
click Edit Target.
Note: This button does not display if this is a Set Properties clause.

Value

Specify the value, which can be an expression containing substitution
keywords, to be set to the variable, component, or property.
To open the Edit Value dialog, click Edit Value. This dialog provides
expression builder functionality for expressions that are names or values
of variables, components, or properties.

Infor Mongoose Application and Form Development | 920

Dialogs-NoTOC

Field

Description

Collection for Target

This field displays a list of collections on the current form. Select a
property from the list.
Note: This field displays only if this is a Set Properties clause.

Run Form parameters
For Run Form event handlers parameters and for editing the Set Variable Values specification, the
caption for this dialog box is Edit Set Variable Pair and restricted to Set Variables clauses.
Field

Description

Target

This list displays either a list of components on the current form (if this
is a Set Component clause), global and form variables for the current
form (if this is a Set Variables clause); or a list of properties on the selected collection on the current form (if this is a Set Properties clause).
Select the variable, component, or property that is the target that receives the output value.
To open the properties dialog box for the selected target component
(if this is a Set Component clause) or variable (if this is a Set Variable
clause), click Edit Target.
Note: This button does not display if this is a Set Properties clause.

Value

Specify the value, which can be an expression containing substitution
keywords, to be set to the variable, component, or property.
To open the Edit Value dialog, click Edit Value. This dialog provides
expression builder functionality for expressions that are names or values
of variables, components, or properties.

Edit Set Values dialog box
For event handlers with the Set Values response type, use this dialog box to set values in variables,
properties, and/or components.
See Set Values response type on page 174.
For In Collection, In Inline List, In List Cache, and Method Call validators, use this dialog box to output
values to properties, variables, and components.
This dialog box provides these options:

Infor Mongoose Application and Form Development | 921

Dialogs-NoTOC

Field/Option

Description

Variables

The button launches the Edit Set Variable Values dialog box,
from which you can add, modify, rearrange, or delete variables
and their corresponding values.
The field displays the variables and the values they are to be set
to. These variable/value pairs must use the syntax variable=v
alue.

Components

The button launches the Edit Set Component Values dialog box,
from which you can add, modify, rearrange, or delete components
and their corresponding values.
The field displays the components and the values they are to be
set to. These component-value pairs must use the syntax compo
nent=value.

Properties

The button launches the Edit Set Property Values dialog box,
from which you can add, modify, rearrange, or delete properties
and their corresponding values.
The field displays the properties and the values they are to be set
to. These property-value pairs must use the syntax property=v
alue.

Do not mark output targets
modified?

When selected, this option invokes the NOMOD keyword, which
instructs Windows client to NOT mark the output target values as
modified.
This is required for cases where you do not want the values of the
target components or properties to be revalidated after the event
handler executes.
Note: This option is not available when this dialog box is launched
from the Edit In Collection Validator Parameters or Edit Inline
List Validator Parameters dialog box, because, in those cases,
the policy for marking output targets is modified from those dialog
boxes.

Edit Timer Actions dialog box
Use this dialog box to set the parameters for the Timer response type. These parameters include:
Parameter

Comments

ACTION

Values are Start and Stop. If you set the value to Stop, you cannot set an INTERVAL.

Infor Mongoose Application and Form Development | 922

Dialogs-NoTOC

Parameter

Comments

INTERVAL

The interval represents the amount of time that is to pass between each "tick" of the
Timer. Each "tick" of the Timer triggers the StdFormTimerTick event. Select a value
from the drop-down list. The system converts this value to milliseconds. Instead of
selecting a value from this drop-down list, you can enter a numeric INTEGER value
in the Event Handler Properties dialog box's Response parameters, for example,
ACTION(Start) INTEGER(5300). In that case, the system internally converts the
value you entered to the nearest option from this drop-down list, for example INTEGER(5000), which is equivalent to 5 seconds.
Note: For forms used in the web client, the minimum valid value is 5000 (milliseconds).
Values less than this are automatically treated as 5000 in the web client.
If you select <None>, the system converts that value to INTERVAL(0). A zero or
negative value does not start the timer.

Edit User Control References dialog box
Use this dialog box to specify assembly, class, and URL information for a user control.
Windows section
Field/Option

Description

Assembly Name

This field specifies the name of the Windows DLL assembly file
to use for the user control.
To browse to and select the DLL assembly file, click the eliipsis
(...) button.
Note: This assembly must contain a class that implements a
Windows (WinForms) user control. The assembly must be available
to be loaded by the smart client at run-time, so it must be added
to any Click-Once deployment packages or included with any
other installation packages.
See About user control components on page 209.

Class Override

By default, looks for the first class in an assembly that inherits from
Mongoose.WinStudio.Runtime.WSUserControlBase (from
WSXRuntime.dll). If an assembly contains more than one of these
classes, specify the full namespace and class name in this field.

Infor Mongoose Application and Form Development | 923

Dialogs-NoTOC

Field/Option

Description

Use Web Control

When this option is selected,the Web section settings are used
for the user control. If this option is selected, the Windows section
settings are disabled. Use this option, in conjunction with the
Control URL field, to allow the user control to operate in both the
smart client and the web client.
When this option is cleared, the user control uses the Windows
section settings for the Windows assembly and the Web section
Control URL setting for the web assembly, if any is specified.

Web section
Field/Option

Description

Assembly Name

This field specifies the name of the web assembly file to use for
the user control.
To browse to and select the web assembly file, click the eliipsis
(...) button.
The web assembly contains a class derived from the WSUserControlBase class or implementing the IWinStudioUserControl interface, and which implements the server-side communications between the Windows client runtime and your code.
The web assembly must be deployed on the Web Server. The
web content can be deployed anywhere.
This assembly can also be imported as an assembly in the Web
User Control Assets dialog box.

Class Override

By default, looks for the first class in an assembly that inherits from
Mongoose.WinStudio.Runtime.WSUserControlBase (from MGWebEngine.dll). If an assembly contains more than one of these
classes, specify the full namespace and class name in this field.

Control URL

This field specifies the URL of the user control, for example, http:
//server/Test.aspx If Relative URL is selected, then only
specify the file name (and any additional path information) from
the base URL, for example Test.aspx.
If the Web User Control Asset option is selected, this must be
one of the HTML pages listed in the Control URL drop-down list.

Infor Mongoose Application and Form Development | 924

Dialogs-NoTOC

Field/Option

Description

Relative URL / Absolute URL
/ Web User Control Asset

Use these options to specify whether the user control reference
is an absolute URL, relative to the base URL, or a web user control
asset.
If you select Relative URL, the actual URL() keyword has as part
of its contents the BASEURL() substitution keyword, which is resolved at runtime.
If Web User Control Asset is selected, it indicates that the page
is from forms metadata for user controls, which is maintained using
the Edit Web User Control Assets dialog box.
Note: Web User Control Assets cannot be modified in a multitenant cloud environment.

Edit ... Validator Parameters
The settings in this topic apply to Alpha, Alphanumeric, Currency, Date, Date-Time, Numeric, and Time
types. Use this dialog box to specify the allowable length, range, or value of these types of validators.
Note: The title/name of this dialog box appears differently, depending on what type of validator you
are working with, as does the availability of options. For example, if you are working with a validator
of type Numeric, then the title of this dialog box displays as Edit Numeric Validator Parameters.
Field/Option

Applies to these validators:

Description

Min (Minimum)

Alphanumeric

This field specifies the minimum value that is acceptable.

Currency
Date
Date-Time
Numeric
Time
Max (Maximum)

Currency
Date

This field specifies the maximum value that is
acceptable.

Date-Time
Numeric
Time
Integer

Numeric

When this option is selected, it signifies that the
values must be integer values.
When this option is cleared, it signifies that values
can be decimal values.

Infor Mongoose Application and Form Development | 925

Dialogs-NoTOC

Field/Option

Applies to these validators:

Description

DBCS

Alpha

This field is used to specify whether the value
can be in the form of double-byte characters
(Double-Byte Character Set), and if so, what format.

Alphanumeric
Numeric

Valid values for this field include these:
•
•
•
•
•
>

Alpha

<

Alphanumeric

>=

Currency

<=

Date

=

Date-Time

<>

Numeric

DOUBLEBYTE
HIRAGANA
KATAKANA
SBKATAKANA
SINGEBYTE

Use these comparison operators to specify how
the input value is to be compared with the set of
valid values.

Time

Edit ... Validator Parameters
The settings in this topic apply to In Inline List and In List Cache types. Use this dialog box to specify
the various parameters for these types of validators.
Note: The title/caption of this dialog box appears differently, depending on what type of validator you
are working with, as does the availability of options. For example, if you are working with a validator
of type In List Cache, then the caption of this dialog box displays as Edit In List Cache Validator
Parameters.

Edit ... Validator Parameters
The settings in this topic apply to Method Call and In Collection types. Use this dialog box to specify
the various parameters for these types of validators.
Note: The title/name of this dialog box appears differently, depending on what type of validator you
are working with, as does the availability of options. For example, if you are working with a validator
of type Method Call, then the title of this dialog box displays as Edit Method Validator Parms.

Infor Mongoose Application and Form Development | 926

Dialogs-NoTOC

Edit Validator Sub Parms dialog box
Use this dialog box to include substitution keywords in the error message for a validator.
The Edit field specifies substitution keywords to include with the string that supplies the error message
for the validator.
The Keep drop-down list specifies the KEEP keyword to include with the validator.
This dialog box can be accessed only by way of the Edit Validators dialog box, by clicking the Parms
icon.

Edit Validators dialog box
Use this dialog box to specify one or more validators for a component. Validators for a component are
executed in the order they are listed.
•
•
•

To create a new validator for a component, click the New icon. The system adds the new validator
to the application's validator set and makes it available to other components.
To add an existing validator to the list for a component, click the Add icon.
To edit a validator, select the validator from the list and click the Edit icon.
Caution: Be careful when editing validators, especially if you are editing a validator that might
be used with other components. Editing a validator for one component affects any other component
that uses that same validator.

•

•

•

To remove a validator from use with a component, select the validator in the list and click the Delete
icon.
Note: Removing a validator here does not delete the validator from the system. It still exists and
can be used with other components. To delete the validator from the system, you must use the
Validators dialog box.
To specify substitution strings for the system to use for parameters during the execution of the
validator, click the Parms (Parameters) icon. This opens the Edit Validator Sub Parms dialog
box. To implement these substitution strings, you must specify corresponding parameters in the
Inheritance > Parameters property field for the component.
To change the sequence of validators in the list (which affects the order in which they execute),
use the Up and Down arrow icons.

Edit Value Expression dialog box
Use this dialog box to specify a conditional action value expression for a component.

Infor Mongoose Application and Form Development | 927

Dialogs-NoTOC

Field

Description

Value Type

This field specifies what type of value is being assigned to the
expression.

Value

This field specifies what value is assigned to the expression. When
the Value Type is:
• Literal, you must specify the value in this field. In this case,
the value is automatically enclosed in quotation marks when
the expression is returned to the parent dialog box.
• VariableValue, you can select the variable from the drop-down
list.
• ComponentValue, you can select the component from the
drop-down list.
• PropertyValue, you can first select the Collection, and then
select the property from this drop-down list.
In this case, the drop-down list does not get populated until
after you specify the Collection.

Global

This option displays only if the Value Type is VariableValue.
When this option is selected, the Value drop-down list displays all
the global variables currently defined. Otherwise, the Value dropdown list displays all the form variables currently defined.

Collection

This field displays only if the Value Type is PropertyValue.
After you select an IDO collection from this drop-down list, the
Value drop-down list is populated with the properties from that
collection.

Edit Variable Binding dialog box
Use this dialog box to set up and maintain specifications for a variable to which a component is bound.
Field/Option

Description

Variable

This field specifies the name of the variable being bound to the component.

Initial Value

This field specifies the initial value of the variable.
You can use Windows client keywords and their parameters to specify values.

Clear on Refresh

When this option is selected, the value of the variable is cleared when
the component is refreshed.
To have a value specified when the component is refreshed, clear
this check box.

Infor Mongoose Application and Form Development | 928

Dialogs-NoTOC

Field/Option

Description

Refresh Value

This field specifies what the value of the variable is to be when the
component is refreshed.
You can use Windows client keywords and their parameters to specify values.

Edit Variable

Click to edit the variable definition.

Edit Web User Control Assets dialog box
See Considerations for adding Web User Control Assets in Multi-tenant Cloud Configurations on page
1155.
Use this dialog box to maintain the web assets in the forms database used to create a unified user
control that can be shared between Windows (smart) and web clients. Web assets include HTML files,
Javascript, CSS, images, and other content that can be presented in a browser, and the option to define
a .NET assembly to facilitate communication between the form and the user control.
The read-only $app folder is used to reference images stored in the forms database as an Image global
object. All other folders represent assets stored in the forms database.
The sohoComponents folder is used to store web assets, such as CSS and JavaScript files, to handle
design themes and scripting.
The sohoxi folder is used to store SoHo user components that are listed in the Toolbox. This folder is
where components are mapped with the required web assets to make it a functional user component.
To import assets for use with the user control, click Import.
To add assets, click New.
This dialog box is accessed, in Design Mode, from the Edit menu.
Note: Web User Control Assets cannot be modified in a multi-tenant cloud environment.

Editing Script in Visual Studio dialog box
This dialog box displays information about a script being edited and provides options for proceeding.
The contents of this dialog box are slightly different for modeless versus modal editing. To specify
modeless editing, select the Visual Studio Modeless check box in the User Preferences editor,
Design Time tab.
This dialog box displays this information about the selected script:
•

The Script Info group box displays this information:

Infor Mongoose Application and Form Development | 929

Dialogs-NoTOC

•

•

• The type of script it is, whether a global script or a form script
• The name of the script
• The version of the script
• The coding language in which the script is written
The Status group box displays this information:
• Whether the script has been modified
• Whether the script has been compiled
The Actions group box provides these options:
Option / Button

Description / Comments

Compile for Debug

(Modeless editing) When this option is selected, the current script is immediately compiled in a separate Debug App domain. You can then switch back
to Visual Studio, set any desired break points and attach WinStudio. To stop
debugging, make additional code changes, and debug again, just stop the
Visual Studio debugger, make the changes, save the changes in Visual
Studio, and reattach to WinStudio. The changed script is automatically recompiled in the Debug App domain when you save the file.
(Modal editing) When this option is selected, the Debug button is enabled.
You can then click Debug to compile the modified script. (In contrast to the
Modeless use of this option, the compilation does not immediately happen,
but only when Debug is clicked.)

Debug

(Modal editing only) This button is enabled only when the Compile for Debug
option is selected. When this button is clicked, it immediately compiles the
script in a separate Debug App domain. You can then switch back to Visual
Studio, set any desired break points and attach WinStudio. To stop debugging, make additional code changes, and debug again, just stop the Visual
Studio debugger, make the changes, save the changes in Visual Studio, and
reattach to WinStudio. The changed script is automatically recompiled in the
Debug App domain when you save the file.

Compile

When clicked, this option immediately compiles the modified script.

VS IDE

When clicked, opens the Visual Studio Integrated Development Environment
that enables you to edit the script.

Close

(Modeless editing only) Closes the dialog box and returns you to the application.

Compile on OK

(Modal editing only) When this option selected, the script is compiled whenever the OK button is clicked.

OK

(Modal editing only) If the Compile on OK option is selected, when you click
this button, the script is compiled and opens in the Visual Studio editing
window.

Cancel

(Modal editing only) When clicked, this dialog box closes without any further
action being taken.

Help

Opens the help topic for this dialog box.

Infor Mongoose Application and Form Development | 930

Dialogs-NoTOC

Editing Versions For Form dialog box
Use this dialog box to design the different versions, listed by device, of the current form. All possible
devices display, and the Select field specifies which devices already have a version designed. To open
the selected version in both Runtime and Design mode, select a device and click Save.
Note: In Runtime Mode, you can only select a version that already exists. In Design Mode, if you select
a device for which a version does not already exist, a new version is created for the selected device.
This dialog box is accessed from a form in Design Mode, through the menu option Form > Definition
> Form Device Type.

Enter a Name for a Theme dialog box
Use this dialog box to specify a name for a new theme or a theme you are copying.
This dialog box is accessible only from the Themes dialog box, by clicking either New or Copy.
After you specify a name, click OK to create the theme and open the Theme Editor.

Enter a Name for the Background Color dialog box
Use this dialog box to specify a name for a background color definition variable.
This dialog box is accessible only by clicking the New button or the Copy button in the Edit Color
dialog box.
Background color definition variables use a "gbgc_" prefix. If you do not supply this prefix as part of
the name, Windows client prepends it automatically when the variable is created.

Event Action Select XML Template from File Server
dialog box
Use this dialog box to select an XML template from a file server to assist in specifying the path and
property map to be used when extracting an XML collection.
This form can be accessed by clicking the Load from File Server button in the Event Action Select
XML Template form.

Infor Mongoose Application and Form Development | 931

Dialogs-NoTOC

Event Handler Parms dialog box
Use this dialog box to set error messages and other specifications for an event handler.
Field/Option

Description

Type Specific Parameters

Click this button to set specifications that are unique to the response
type for this event handler.

Error Message

Enter the message to be displayed when the event handler returns
an error. You can include string names and any of the WinStudio
substitution keywords.

Success Message

Enter the message to be displayed when the event handler succeeds. You can include string names and any of the WinStudio
substitution keywords.

Only When Current Collection
Is

Select the name of the collection to which this event handler applies.
Normally, event handlers execute regardless of the currently active
collection or the specific collection operation that generated the
event.

Only When Platform

Select the name of the platform (Windows, Web, or All) to which
this event handler applies. Normally, event handlers execute regardless of the currently active platform.

Ignore Failure

Select this check box to classify any result returned by this event
handler as a success. Use this setting if you want subsequent event
handlers to continue to execute even if this event handler returned
a failure.
Otherwise, clear this check box. Use this setting if you want the
failure of this event handler to stop the execution of subsequent
event handlers.

Message on Status Line

Select this check box to display the error and success messages
in the application status line. Clear this check box to display these
messages in a pop-up dialog box.

Disabled

Select this check box to temporarily disable the event handler.
WinStudio will not run a disabled event handler, even if its other
execution criteria are satisfied. Clear this check box to enable the
event handler.

Event Handlers dialog box
Use this dialog box to create, copy, edit, delete, enable or disable event handlers for the current form.
You can also change the order in which event handlers execute and find strings in event handler
specifications.

Infor Mongoose Application and Form Development | 932

Dialogs-NoTOC
Note: This topic refers to the system of Windows client form events, and not to the Application Event
System, in which events and handlers are defined for application-wide use.
About the display
The content of the event handlers list depends on how you open the dialog box:
•

If you open the dialog box from the Events sheet for the component, with no event name specified,
the list displays all event handlers for the form.
• Similarly, if you open the dialog box from the Windows client menu bar (Edit > Event Handler),
the list displays all event handlers for the current form.
• If you open the dialog box from the Events sheet with an event specified, this dialog box lists only
the event handlers that are associated with the specified event. In this case, the title bar of the
dialog box identifies the event whose event handlers are listed.
Note:
• The dialog box indicates whether each event handler is defined on the base form or the extended
form.
See About creating new forms by extension on page 136.
•

•

Event handlers on the base form are grayed out, indicating that they are not available for editing
on this form. To edit the event handler for the extended form, select it and click Edit. In the Event
Handler Properties dialog box, first set the From Base Form property to False, and then you
can edit it like any other event handler.
This causes the event handler to be associated with the extended form. Be aware that, if you have
a base form event with multiple handlers, and you edit one handler this way, all the other handlers
defined for that event also become associated with the extended form.

The grid in this dialog box displays this information about each event handler:
Column

Description/Comments

Event

Indicates the internal name of the event for which the handler is defined

Seq

Indicates the order in which the event handler executes
This zero-based list number is significant only when an event has multiple event
handlers. You can change this sequence using the Up and Down arrows at the
bottom of the dialog box.

Type

Indicates what type of event handler is defined

Disabled

When selected, indicates that the event handler is not to be executed

Response

Indicates how Windows client is to respond when the event handler executes
This typically consists of a set of parameters that define what the handler is to
do.

Dialog box options
This dialog box includes these action buttons:

Infor Mongoose Application and Form Development | 933

Dialogs-NoTOC

Button

Description/Comments

Event filter

To specify a single event for which you want to view the defined handlers, use
this drop-down list.
To view all event handlers, select [ALL] .

OK

To finish working in the dialog box and save your changes, click this button.

New

To create a new event handler, click this button.
This button launches the Event Handler Properties dialog box, which allows
you to define the new event handler.

Copy

To copy an event handler, select the handler to copy and click this button.
This is useful when you want to base a new event handler on an existing handler
with changes.
This button launches the Event Handler Properties dialog box, populated with
the attribute for the handler you are copying.

Edit

To edit an existing event handler, select the handler and click this button.
This button launches the Event Handler Properties dialog box, populated with
the attributes for the specified handler.

Delete

To delete an event handler, select the handler and click this button.

Cancel

To cancel any changes made and close the dialog box, click this button.

Handlers

To filter the list of event handlers for the event generated by a handler of the
type, click this button.
This is enabled only when the current handler is of the type Generate Event.

Find

To search the list of event handlers for a particular handler, click this button.
You can search by keyword, method, parameter, or other specifications.

Up and Down arrows

To change the sequence order of event handlers, use these buttons.

Help

To view the help topic for this dialog box, click this button.

These buttons are enabled only if an event has multiple handlers.

Extended Form Details dialog box
Use this display-only dialog box to view details about the extended form you are working with in Design
Mode.
Field/Tab/Option

Description/Comments

Name

Displays the name of the extended form
Note: This is the actual form name, and not the form caption.

Infor Mongoose Application and Form Development | 934

Dialogs-NoTOC

Field/Tab/Option

Description/Comments

Scope Type

Specifies whether the scope under which the extended form was created
was:
•
•
•

Vendor Default
Site
Group

Scope Name

Specifies the name of the group or user for which the form was created.
This field is populated only if the form was extended at the Group level.

Base Form

Displays the name of the form on which the current form is based (from
which it was extended)

Hierarchy tab

Displays both the "ancestors" and the "descdendants" of the current form
If the extended form is based on a Vendor Default version, then the base
form is the only ancestor. You can have descendants only if you have an
extended form that is itself based on another extended form.

Components tab

Lists all the components on the form by component name This list subdivides the list into lists of the components on any ancestor versions of the
form (one sublist for the base form and one each for any other extended
versions in the hierarchy)and the list of components unique to this extended
version.

Event Handlers tab

Lists all the event handlers defined for the form This list subdivides the list
into lists of the event handlers on any ancestor versions of the form (one
sublist for the base form and one for each other extended version in the
hierarchy) and the list of event handlers unique to this extended version.

Form Variables tab

Lists all the form variables defined for the form This list subdivides the list
into lists of the variables on any ancestor versions of the form (one sublist
for the base form and one for each other extended version in the hierarchy)
and the list of variables unique to this extended version.

This form is accessed by clicking the ellipsis (…) button for the Base Form Name property in the Form
property sheet.

Find dialog box, Object Viewer
Use this dialog box to find objects by name in the Object Viewer on page 23 tree. Searches use implied
wildcards and are not case-sensitive.

Infor Mongoose Application and Form Development | 935

Dialogs-NoTOC

For Your Convenience dialog box
Use this dialog box as a text editor to maintain the information displayed.

Form Component List dialog box
Use this dialog box to view all the components on the current form and to edit certain types of properties.
Each row in the list represents a component on the form. Each column displays a property for the
component.
Column/Property

Description/Comments

Name

Specifies the internal name of the component, which must be unique to
the form

Type

Specifies what type of component it is

Data Source

Specifies the source of data to which the component is bound

Container

Specifies the container, if any, to which the component belongs

Tab Order

Specifies the tab order number of the component

Event

Specifies the event to be generated when the user interacts with the component in run time

Pane

(Multiview and query forms only) Indicates the pane in which the component
is displayed
Valid values include:
•
•

Error?

First: The Grid View or the top pane
Second: The Detail View or the bottom pane

Indicates whether an error was generated when the component was created
OK indicates that no errors were generated.

Component Class

Specifies the component class on which the component is based, if any

Enabled When

Specifies the conditions under which this component is enabled, if any

Visible When

Specifies the conditions under which this component is visible, if any

Required When

Specifies the conditions under which this component is required, if any

From Base Form

Indicates whether the component takes its value and properties from a
base form

From Master Device

Indicates whether the component is part of the master device type for the
form

Note: In the case of a form that has been created as an extension of another form, this dialog box
displays the components in two lists:

Infor Mongoose Application and Form Development | 936

Dialogs-NoTOC
•

One for the components that take their values from the extended form (From This Form)
These are the components that are available for editing on the form.

•

One for the components that take their values from the original form (From Base Form)
These components are disabled and unavailable for editing unless you "unlock" them.

You can also copy and paste data from the grid into a spreadsheet for further searching or sorting of
the entire list of components on a form.
Editing components from this dialog box
To edit properties of a component, select the row for the component you want and then click Edit.
WinStudio launches the Form Component Properties dialog box, which you can then use to edit the
selected component's properties.
Note: When working with a form that has been created as an extension of another form, you can
override the default setting that prevents you from changing the properties for a component belonging
to the original (base) form. To do this, in the Form Component Properties dialog box, first reset the
From Base Form property to False; and then you can modify the component's other properties.

Form Definition Templates dialog box
Use this dialog box to edit or delete a form template. All the form templates stored in the templates
database are included in the list.
To edit the IDO properties included as components in a form template, select the form template you
want and click Edit.
To delete a form template, select the one you want and click Delete.

Form properties sheet
Use the Form properties sheet to set form and collection properties. These are properties that are used
by the form as a whole to:
•
•
•
•

Control the way the form displays
Specify aspects of the form's behavior
Define what data (properties) are available for use by the various form components
Specify the help topic for the form

The Form tab
The Form tab displays properties of the current form and includes options to:

Infor Mongoose Application and Form Development | 937

Dialogs-NoTOC
•
•

Sort properties by category
Sort properties alphabetically

For information about a specific form property, click the property row, and then right-click and select
Help from the context menu.
The Collections tab
The Collections tab displays the IDO collections available for use on the form, using a hierarchical
'tree' structure.
Note: This tab is not available to users with editing permissions of None or Basic.
A form can include data from both primary and secondary collections.
•
•

The primary collection is defined at the time the form is created. You cannot add or delete the
primary collection.
This tab also includes options to:
• Add a secondary collection, making its properties available on the form
• Delete a secondary collection
• Specify a record cap that applies only to the selected collection.
Note: This setting is subject to any system-wide record cap overrides that might be in effect.

You can expand the node for each collection to display the properties for the collection. You can then
click-and-drag these properties to the form and drop them there to create new components.

Get Database Table Schema dialog box
Use this dialog box to select a database table, the schema of which to use as the basis of a new
table/IDO/form. This is often helpful when you want to create a new table/IDO/form that is similar to
an existing one.
Note: When you select a database table from the list and then click OK, any existing rows in the
name/property grid are cleared before the schema is imported.

Global Object Filters, Object Viewer
Use the Filter(s) for Global Objects section to filter the contents of the objects tree by name.
To filter for a specific type of global object, select the global object you want in the Type drop-down
list. To filter all global objects using the filter criteria, specify [ALL] in the Type field.
Specify the string by which to filter in the Filter field. Filtering uses implied wildcards and is not
necessarily case-sensitive. The filter string "class" matches ClassName, PropertyClassName, and
CostCodeClass. If the filter is based on an expression, select the Regular Expression option.

Infor Mongoose Application and Form Development | 938

Dialogs-NoTOC
To perform the actual search-and-filter operation, click Apply.
To clear the filter criteria and settings so you can perform a new search, click Clear.

Identify Your Editing Scope
If you have Site Developer editing permissions, use this dialog box to set or change your editing scope.
Field/Option

Description

Scope

The level at which your design changes are saved. As a user with Site
Developer editing permissions, you can save your changes as a User,
Group, or Site version.

For User/Group

If Scope is User or Group, the name of the user or group for whom you
are creating this version.

IDO String Does Not Exist dialog box
This dialog box displays when one or more of the properties to be included on the new form does not
have a component caption. Use this dialog box to create a string to use for the component caption. (A
property has no component caption if you did not enter a caption on page 3 of the New Form Wizard
and if the associated IDO property has a string name that is not found in the strings table in the forms
database.)
Field/Option

Description

Yes / No

To create a string for the component caption, enter the string and click
Yes.
If you do not want to create a string, click No.

For additional IDO strings

If other properties for the new form are also missing captions, indicate
how to handle them:
• Prompt: Display this dialog box for every property that is missing
a component caption.
• Never: Do not display this dialog box again and do not create
any strings.
• Always: Automatically create strings for every property that is
missing a component caption. Windows client creates strings automatically when you add 's' as a prefix to the IDO property's Label
String ID attribute.

Infor Mongoose Application and Form Development | 939

Dialogs-NoTOC

Images
Use this dialog box to import images to the forms database. This makes it possible to transport the
image file with the form and eliminates the need to call the image from an external location.
Beneath the image display, the customized scope is displayed.
Field/Button/Option

Description/Comments

File

Lists the image files that have been added.
From this list, you can select a file to import, export, or delete.

Display Option

Specifies one of these display options for the default display of
the image: Fit, Fill, or Actual.
See Image Display Property on page 1109.

Image display area

Displays the currently selected image.
To change the apparent resolution, select an option from the Display Options field.

OK

Click to close the dialog box.

New

Click to begin a new entry, and to open the Open File dialog box
to select the new entry.

Import

Click to import a replacement image and update the currently selected image using the Open File dialog box.

Export

Click to export the image using the Open File dialog box.

Delete

Click to remove the image.

Help

Click to open the online help for this dialog box.

Import Files and Folders dialog box
This dialog box is used to add or replace web asset global objects, including:
•

•

Browser assets associated with a web user control or a .NET assembly that are associated with
the user control and may be used to provide the server-side Code Behind type logic for a user
control's browser component.
Legacy WinForms and Web Client user control assemblies.

The system first looks for the assemblies in the forms metadata, then attempts to find them on the disk.
The Source Folder field is used to select the location of files stored on the file system. The Target
Path field indicates the root path to use for imported files. The effective path for an imported file is
constructed by adding the Target Path to the Source Folder.
To select a new folder, click Source Folder.

Infor Mongoose Application and Form Development | 940

Dialogs-NoTOC
To select files and folders to import, click Add Files. Selecting a folder selects all files within the folder.

Middleware Update/Load Override dialog box
Use this dialog box to specify parameters for the Windows client UPD, INS, DEL, or PQ keywords.
These keywords set up custom processing in the IDO that overrides the corresponding standard
operation on the collection.
Field/Option

Description

UPD keyword

Perform custom processing in the IDO for updating existing records.

INS keyword

Perform custom processing in the IDO for inserting new records

DEL keyword

Perform custom processing in the IDO for deleting records

PQ keyword

Perform post-query processing in the IDO before the data is returned from
the middleware to . This processing can be used with either UPD, INS, or
DEL.

New Data Maintenance Wizard (Windows client
version)
Note: This topic applies only the Windows client version of the New Data Maintenance Wizard. There
exists a corresponding version of this wizard for the web client.
See New Data Maintenance Wizard (web client version) on page 1367.
Use the New Data Maintenance Wizard to quickly and easily create forms with their required SQL
tables, IDOs, and components. This wizard can be especially helpful when you want to build a completely
new form without having to create the infrastructure for it first.
To access this wizard, you must first be in Design Mode. To start the wizard then, on the Design Mode
toolbar, click the New Data Maintenance Wizard icon.

New Data Maintenance Wizard, Finish page
Use this page of the wizard to optionally create a template from this new form and to finish the process
and have WinStudio create the SQL table, the IDO, and optionally the form.
To save a form template based on the new form definition, select Save Template. Otherwise, clear
this check box.

Infor Mongoose Application and Form Development | 941

Dialogs-NoTOC
When you click Finish, the wizard:
•
•
•
•
•
•
•

Creates the table, columns, primary key definitions, IDO, properties, template data, and (optionally)
the new form
Empties the IDO metadata cache and saves IDO metadata for the form to the objects database
Saves the template to the templates database (if the Save Template option was selected)
Saves the form definition metadata to the forms database
Runs the SQL Tables form filtered for the new table
Runs the IDOs form filtered for the new IDO
Opens the new form

Windows client initially displays the form in Runtime Mode. This allows you to test use the form to enter
and save data immediately.
You can, however, go to Design Mode and modify the form layout and definition as desired before you
test it.

New Data Maintenance Wizard Options dialog box
Use this dialog box to set these options for the table, IDO, and form to be created:
Field / Option

Description / Comments

IDO description

Specify a description for the IDO to be created.
This description is then displayed when the IDO is specified in the Description field of the IDOs form.

Form description

Specify a description for the form to be created.
This description is then displayed in the Description property field of the
Form property sheet in Design Mode.

Form caption

Specify a caption for the form to be created.
If you do not specify a form caption here, the system defaults to a caption
based on the form name.

Sort Columns Alphabetically

When selected, this option causes all columns/properties you create for
the IDO to be sorted and displayed alphabetically on the IDOs form.

New Form Wizard (Design Mode)
Note: This topic describes the New Form Wizard as it exists in and operates in the Windows client
(in Design Mode). The web client has a similar wizard for creating new forms. See Form Wizard Selection
on page 1366.

Infor Mongoose Application and Form Development | 942

Dialogs-NoTOC
In the Windows ("smart") client, use the New Form Wizard to create forms. You can create forms that
are either completely new or based on an existing form.
To access this wizard, you must first be in Design Mode. To start the wizard once in Design Mode,
select Form > Definition > New or click the New Form Wizard icon on the toolbar.

New Form Wizard, Create New Form page
Use this page of the New Form Wizard to make the basic specifications for a new form.
Required specifications
You must specify:
•

The Name of the form
If you are using an existing form's template, you can select it from the drop-down list. If you are
not using an existing template, you can type in this field the name for the new form.

•
•

The type of form (Form Type) you want to create
The Data Source for the form
This is the IDO collection which is to serve as the primary collection for the form.
Note: For Build-from-scratch forms only, this field is not required. All other types of form require
that you specify a primary IDO collection.

Optional specifications
Optionally, you can also specify:
•

The default Form Layout
For MultiView forms only, this option determines which Grid View display option the wizard is to
use by default. You can override this setting later in the process.

•

What Device Type is to be the default device type
This option assumes that your system has more than one device type defined.
See About Devices and Derived Forms on page 134.

•

The name of the Query Form to be associated with this form
This can be either the name of an existing query form, or you can type in the name of the query
form you intend to create for this form.

•

Descriptive Comments to be saved with the form's template
These comments do not display anywhere in the created form. They are used and stored only with
the form definition/template.

Next steps
When you click Next, the page you see next depends on the type of form you are creating.

Infor Mongoose Application and Form Development | 943

Dialogs-NoTOC

New Form Wizard, Extend Form page
Use this page to provide the specifications to create a new form by extending an existing form.
On this page, you must provide the name for the new form and specify the existing form on which the
new form is to be based.
When you do this, Windows client automatically provides a recommended Description, which you can
modify.
To load the new form when a request to open the base form is received, select the Replace Base
Form check box.

New Form Wizard, Finish page
Use this page of the wizard to create a template from this new form (optional) and to finish the process
and have Windows client create the form.
To save a form template based on the new form definition, select Save Template. Otherwise, clear
this check box.
Note: This option is not available if you are creating a form by extending an existing form.
When you click Finish, the wizard:
•
•
•
•
•

Creates the form as defined, with all included properties represented by and bound to form
components
Empties the IDO metadata cache and saves IDO metadata for the form to the objects database
Saves the template to the templates database (if the Save Template option was selected)
Saves the form definition metadata to the forms database
Launches the new form in Design Mode.

Windows client initially displays the form in Design Mode so that you can modify the form definition as
desired before you test it.
You can, however, immediately go to Runtime Mode and use the form to enter and save data.

New Form Wizard, Link Properties page
Use this page of the wizard to define the link between the primary collection of a form and a subcollection
on that form.
This is accomplished by selecting (typically) corresponding properties from the primary collection and
the subcollection and then defining a link between them (Add Link). This allows the two collections
both to display as appropriate on the form, with the proper relationship established.

Infor Mongoose Application and Form Development | 944

Dialogs-NoTOC
Windows client displays new links in the Linkage List.

New Form Wizard, Properties page
Use this page of the wizard to specify attributes for each IDO property that is to be represented on the
newly created form.
At the same time, on some form types (Multiview forms and Detail Only forms, for example), you can
use this page to define form pages and/or a notebook, if the property is intended to operate within one
of those container types.
Basic property specifications
Note: Above the top right of the Properties grid, the wizard indicates whether you are working with
the properties of the primary collection or a subcollection.
On this page, you can specify:
•

The order in which the properties are listed.
To adjust the order, select a property and then use the appropriate buttons to the right of the
Properties grid.
Note: This specification determines the original order in which the components display on the
newly created form. You can also rearrange things to your liking after the basic form is created.
This order is also the default tab order.

•

A caption for each component.
The Caption property specifies the label for a component on the resulting form.
To specify a caption, enter a string of text or select an existing string from the Caption drop-down
list. If the string label does not already exist, the wizard provides the opportunity to create it here.
For more information, see About Strings on page 275.

•

Containment for each component.
Use the Containment option to have the component display on a form page or a notebook tab.
Note: Before you can use this option, you must first define the container in which you want the
component to appear.
These options are not available for Grid Only forms or Query forms.
See Edit Contained Components Dialog Box on page 894.

•

Whether to include the property in the Grid View (Include in Grid).
(For Multiview forms only) To have the selected property represented by a column in the Grid View
as well as by a component in the Detail View, select this option. To have the property represented
in the Detail View but not in the Grid View, clear this option.

•

A component class for each component.
If a Component Class is specified for the property, the component class is applied to the property's
component in the resulting form. If the component class requires or accepts parameters, specify
those parameters in the Component Class Parameters field.

Infor Mongoose Application and Form Development | 945

Dialogs-NoTOC

•

Note: This option is not available for Query forms.
Where the properties will be used:
• Properties selected to Include in Primary appear on the Primary Criteria tab.
• Properties selected to Include in Additional are available for queries specified using the
Additional Criteria tab.
• Values for properties selected to Include in Results are returned with the query results and
display in the Results grid.
Note: These options are available only for Query forms.

Container specifications
Before you can specify Containment for any property, you must first specify at least one container.
You have two options for containers in the wizard:
•

To create the form as a multi-page form, you can specify one or more Form Page components.
You can create as many form pages here as you want. When the form is then created, these pages
are also created and are ready for use as such immediately. You must specify a Name and a
Sequence number for each page you create.

•

To create the form with a notebook and tabs, you can specify only one notebook.
You can create as many tabs for the notebook as you would like, but if you want multiple notebooks,
all other notebooks must be created after the initial form creation.
For each tab of the notebook, you have the option to specify a Notebook Tab Caption here. If the
string for the caption does not already exist, you have the option to create it here. If you do not
specify a caption, the wizard uses a default caption (Tab1, Tab2, etc.), which you can later change.

New Form Wizard, Select Properties page
Use this page of the wizard to specify which IDO properties become components on the resulting form.
Where and how these properties become components depends on what type of form you are creating.
On a MultiView form, for example, properties specified for inclusion on this page become both columns
in the Grid View and other types of components in the Detail View. The type of component it becomes
in the Detail View depends on what type of property it is to begin with. But, regardless of the type that
WinStudio creates the property component as, you can use Design Mode to change the component
type later.
The exception to this rule is the query form, where the included properties do not become components,
but rather the criteria that users can use to perform a query.
This page displays two lists of properties:
•
•

Properties in the left pane are not assigned to any components (or available as query criteria) in
the newly created form.
Properties in the right pane, the "included" list, become bound to appropriate components (or
available as criteria) in the new form.

Infor Mongoose Application and Form Development | 946

Dialogs-NoTOC
Most properties are bound to Edit components. If there is a list source associated with a property,
it is bound to a combo box component. To use a different component type on the form, you can
change the component type later, after the basic form has been created.
On this page, you can both add properties to the list to be included and remove properties from that
list.
See Moving properties to the "included" list on page 1245 and Removing properties from the "included"
list on page 1247.

New Form Wizard, Select Report Properties page
Use the Select Report Properties page of the New Form Wizard to:
•
•
•
•

Define what regions and subregions the wizard is to create for the new Report-type form.
Specify whether the Report form should use a portrait orientation or a landscape orientation.
Create a background task for printing the report and optionally specify the size of paper to use.
Select properties by which to order, group, and display the report data.

This page displays only if you are creating a Report-type of form.

New Form Wizard, Subcollections page
If one of the properties on the new form is a subcollection property, use this page of the wizard to
identify the subcollection information. Typically, the subcollection property is a grid component on the
resulting Multiview form.
Group Box

Field / Option

Description / Comments

Specify Child and Parent
Property and Click Add

Primary Collection

Specify a property in the primary collection to be
linked to a subcollection property.

Specify Child and Parent
Property and Click Add

Subcollection

Specify a property in the subcollection to be
linked to a primary collection property.

Property Pairs Linking
Primary to Subcollection

Add button
